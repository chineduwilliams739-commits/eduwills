name: Deploy EDUWILLS to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Apply stable EDUWILLS implementation
        shell: bash
        run: |
          set -euxo pipefail
          node --check scripts/repair-eduwills-next.mjs
          node scripts/repair-eduwills-next.mjs
          node --check scripts/publish-education-news.mjs
      - name: Verify final implementation
        shell: bash
        run: |
          set -euxo pipefail
          test -f lib/aiAccess.ts
          test -f app/dashboard/ai/page.tsx
          test -f components/EducationFeed.tsx
          test -f public/education-news.json
          node - <<'NODE'
          const fs = require('fs');
          const quiz = fs.readFileSync('app/dashboard/quiz/page.tsx','utf8');
          const client = fs.readFileSync('lib/quizAiClient.ts','utf8');
          const dashboard = fs.readFileSync('app/dashboard/page.tsx','utf8');
          if (!client.includes("const CACHE = 'v22-broad-book-knowledge-cache-first'")) throw new Error('New cache namespace missing');
          if (!client.includes('readSharedCache')) throw new Error('Shared cache reader missing');
          if (!client.includes('STRICT BOOK SCOPE')) throw new Error('Strict book scope missing');
          if (!client.includes('allocations = selected.map')) throw new Error('Multi-book allocation missing');
          if (!client.includes('The Lekki Headmaster')) throw new Error('Lekki Headmaster knowledge pack missing');
          if (!client.includes('Sànyà')) throw new Error('Sànyà knowledge pack missing');
          if (!quiz.includes("searchBookAuthors('title', raw)")) throw new Error('Quiz title search wiring missing');
          if (!quiz.includes('canvas.width = 1400; canvas.height = 1000;')) throw new Error('Fresh result image format missing');
          if (!dashboard.includes('<EducationFeed />')) throw new Error('Dashboard education feed missing');
          console.log('EDUWILLS stable implementation verification passed');
          NODE
      - name: Build application
        env:
          NEXT_PUBLIC_RECAPTCHA_ENTERPRISE_SITE_KEY: ${{ secrets.NEXT_PUBLIC_RECAPTCHA_ENTERPRISE_SITE_KEY }}
        run: npm run build
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
