# EduWills deployment notes

Quiz runtime hardening is included in the current main branch. This marker intentionally triggers the Pages deployment so the runtime error diagnostics are included in the published build.
