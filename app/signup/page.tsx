'use client';
import { useState } from 'react';
import { ArrowLeft, Check, LockKeyhole, ShieldCheck } from 'lucide-react';
import { createUserWithEmailAndPassword, sendEmailVerification } from 'firebase/auth';
import { doc, setDoc, serverTimestamp } from 'firebase/firestore';
import PhoneInput, { isValidPhoneNumber } from 'react-phone-number-input';
import 'react-phone-number-input/style.css';
import { auth, db } from '@/lib/firebase';
import { getEduWillsVisitorId, trackEduWillsEvent } from '@/components/AnalyticsTracker';
import { createPublicId } from '@/lib/publicIdentity';
const BASE='/eduwills';
const options=[{id:'senior',name:'Senior Secondary',price:3000,status:'Available now'},{id:'junior',name:'Junior Secondary',price:3000,status:'Available now'},{id:'primary',name:'Primary',price:2000,status:'Available now'},{id:'book',name:'Book Learner',price:4000,status:'Available now'}];
export default function SignUpPage(){const[selected,setSelected]=useState<string[]>([]);const[phone,setPhone]=useState<string|undefined>('');const[message,setMessage]=useState('');const[showPassword,setShowPassword]=useState(false);const[saving,setSaving]=useState(false);function toggle(id:string){const item=options.find(x=>x.id===id);if(!item||item.status!=='Available now')return;setSelected(c=>c.includes(id)?c.filter(x=>x!==id):[...c,id])}
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMessage('');const form=new FormData(e.currentTarget);const fullName=String(form.get('fullName')||'').trim();const username=String(form.get('username')||'').trim().toLowerCase();const email=String(form.get('email')||'').trim().toLowerCase();const password=String(form.get('password')||'');const confirm=String(form.get('confirmPassword')||'');if(!email||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){setMessage('Enter a valid email address.');return}if(!phone||!isValidPhoneNumber(phone)){setMessage('Enter a valid phone number and select the correct country code.');return}if(!fullName||!username||password.length<6){setMessage('Please complete your name, username and password. Password must be at least 6 characters.');return}if(!/^[a-z0-9._-]{3,30}$/.test(username)){setMessage('Username can contain only letters, numbers, dots, underscores and hyphens.');return}if(password!==confirm){setMessage('The passwords do not match.');return}if(!selected.length){setMessage('Choose an available learning category.');return}setSaving(true);await trackEduWillsEvent('signup_started',{categories:selected});let uid='';try{const credential=await createUserWithEmailAndPassword(auth,email,password);uid=credential.user.uid;await sendEmailVerification(credential.user);let acquisitionSource='unknown';let acquisitionLandingPath=`${BASE}/signup/`;let acquisitionVisitorId='';try{acquisitionSource=localStorage.getItem('eduwills_first_source_v2')||'unknown';acquisitionLandingPath=localStorage.getItem('eduwills_landing_path_v1')||acquisitionLandingPath;acquisitionVisitorId=getEduWillsVisitorId()}catch{}const publicId=createPublicId();try{localStorage.setItem('eduwills_public_id',publicId);localStorage.setItem('eduwills_current_uid',uid)}catch{}const userData={uid,fullName,username,email,authEmail:email,phone:phone.replace(/^\+/,''),phoneE164:phone,publicId,publicIdVersion:1,categories:selected,activated:false,activationStatus:'inactive',activationExpiresAt:null,emailVerified:false,createdAt:serverTimestamp(),acquisitionSource,acquisitionLandingPath,acquisitionVisitorId};
      try{await setDoc(doc(db,'users',uid),userData);await setDoc(doc(db,'phoneIndex',phone),{uid,authEmail:email,username,phoneE164:phone,publicId},{merge:true});await setDoc(doc(db,'emailIndex',email),{uid,username,phoneE164:phone,publicId},{merge:true});await setDoc(doc(db,'publicUserIndex',publicId),{uid,username,fullName,publicId,createdAt:serverTimestamp()});}catch(profileError){console.warn('Signup profile persistence will be repaired after verification.',profileError)}
      try{await trackEduWillsEvent('signup_completed',{categories:selected,publicId})}catch{}
      try{localStorage.setItem('eduwills_current_user',username);localStorage.setItem('eduwills_current_uid',uid);localStorage.setItem('eduwills_public_id',publicId)}catch{}
      window.location.replace(`${BASE}/verify-email/`);
    }catch(error:unknown){const code=error instanceof Error?error.message:'';if(code.includes('email-already-in-use'))setMessage('That email address is already registered.');else if(code.includes('weak-password'))setMessage('Password must be at least 6 characters.');else if(uid&&auth.currentUser?.uid===uid){window.location.replace(`${BASE}/verify-email/`);return}else setMessage('We could not create your account. Please check your details and try again.');setSaving(false)}}
return <main className="min-h-screen bg-paper px-4 py-5 sm:px-6 sm:py-8"><style>{`.eduwills-phone .PhoneInput{display:flex;align-items:center;width:100%;min-height:54px}.eduwills-phone .PhoneInputCountry{position:relative;display:flex;align-items:center;gap:7px;padding:0 12px;border-right:1px solid #cbd5e1;min-height:54px;background:#f8fafc;flex:0 0 auto}.eduwills-phone .PhoneInputCountrySelect{position:absolute;inset:0;width:100%;height:100%;cursor:pointer;opacity:0}.eduwills-phone .PhoneInputCountrySelectArrow{margin-left:3px;width:8px;height:8px;border-right:2px solid #475569;border-bottom:2px solid #475569;transform:rotate(45deg) translateY(-2px)}.eduwills-phone .PhoneInputInput{min-width:0;flex:1;border:0;background:transparent;padding:15px 14px;outline:0;font-size:.95rem;color:#0f172a}.eduwills-phone .PhoneInputInput::placeholder{color:#94a3b8}.eduwills-phone .PhoneInputCountryIcon{width:24px;height:18px;overflow:hidden}`}</style><div className="mx-auto w-full max-w-5xl"><a href={`${BASE}/`} className="mx-auto flex w-fit items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Back to EDUWILLS</a><div className="mx-auto mt-6 grid w-full max-w-5xl overflow-hidden rounded-[1.5rem] border border-slate-200 bg-white shadow-soft sm:mt-8 sm:rounded-[2rem] lg:grid-cols-[.82fr_1.18fr]"><aside className="hidden bg-ink p-10 text-white lg:block"><div className="text-2xl font-black">EDUWILLS</div><div className="mt-16 max-w-sm"><div className="text-xs font-black uppercase tracking-[.2em] text-cyan-200">Create your learning account</div><h1 className="mt-4 text-4xl font-black leading-tight">Start free. Activate when you're ready.</h1><p className="mt-5 leading-7 text-slate-300">Registration is free. Quiz, History and EDUWILLS AI become available after activation.</p></div><div className="mt-12 space-y-4 text-sm text-slate-300">{['Free account creation','Email verification only','Activate later with WilliToken','Get a unique account link'].map(item=><div key={item} className="flex items-center gap-3"><span className="grid h-6 w-6 place-items-center rounded-full bg-white/10 text-cyan-200"><Check size={14}/></span>{item}</div>)}</div></aside><section className="min-w-0 p-5 sm:p-8 md:p-10"><div className="mx-auto w-full max-w-xl"><p className="text-xs font-black uppercase tracking-[.2em] text-eduBlue">Free registration</p><h2 className="mt-2 text-3xl font-black tracking-tight text-ink">Join EDUWILLS</h2><p className="mt-2 text-sm leading-6 text-slate-500">Use a real email address so we can verify your account and recover it if you forget your password.</p><form className="mt-7 space-y-5" onSubmit={submit}><div className="grid min-w-0 gap-4 sm:grid-cols-2"><label className="min-w-0 text-sm font-bold text-ink">Full name<input name="fullName" required className="mt-2 block w-full min-w-0 rounded-xl border border-slate-200 bg-paper px-4 py-3 outline-none focus:border-eduBlue" placeholder="Your full name"/></label><label className="min-w-0 text-sm font-bold text-ink">Username<input name="username" required className="mt-2 block w-full min-w-0 rounded-xl border border-slate-200 bg-paper px-4 py-3 outline-none focus:border-eduBlue" placeholder="Choose a username"/></label></div><label className="block text-sm font-bold text-ink">Email address<input name="email" required type="email" autoComplete="email" className="mt-2 block w-full rounded-xl border border-slate-200 bg-paper px-4 py-3 outline-none focus:border-eduBlue" placeholder="you@example.com"/><span className="mt-1 block text-xs font-normal text-slate-400">A verification link will be sent here.</span></label><label className="block text-sm font-bold text-ink">Phone number<div className="eduwills-phone mt-2 overflow-hidden rounded-xl border border-slate-200 bg-paper shadow-sm"><PhoneInput international defaultCountry="NG" countryCallingCodeEditable={false} value={phone} onChange={setPhone} placeholder="Enter your phone number"/></div><span className="mt-1 block text-xs font-normal text-slate-400">Choose your country from the country selector.</span></label><div><div className="mb-3 flex items-center justify-between"><span className="text-sm font-bold text-ink">Choose learning category</span><span className="text-xs font-bold text-slate-400">Available categories</span></div><div className="space-y-2">{options.map(item=>{const active=selected.includes(item.id);const locked=item.status!=='Available now';return <button type="button" key={item.id} onClick={()=>toggle(item.id)} className={`flex w-full items-center justify-between rounded-xl border p-3.5 text-left transition sm:p-4 ${active?'border-blue-200 bg-blue-50/70':'border-slate-200 bg-white'} ${locked?'cursor-not-allowed opacity-60':'hover:border-blue-200'}`}><span className="flex min-w-0 items-center gap-3"><span className={`grid h-5 w-5 shrink-0 place-items-center rounded-md border ${active?'border-eduBlue bg-eduBlue text-white':'border-slate-300'}`}>{active&&<Check size={13}/>}</span><span className="min-w-0"><span className="block text-sm font-bold text-ink">{item.name}</span><span className="block text-xs text-slate-400">{locked?<span className="inline-flex items-center gap-1"><LockKeyhole size={11}/> {item.status}</span>:`Activation: ₦${item.price.toLocaleString()}`}</span></span></span><span className="shrink-0 pl-2 text-xs font-bold text-slate-500">{locked?'Locked':'Selected'}</span></button>})}</div></div><div className="grid min-w-0 gap-4 sm:grid-cols-2"><label className="min-w-0 text-sm font-bold text-ink">Password<div className="relative mt-2"><input name="password" required minLength={6} type={showPassword?'text':'password'} className="block w-full min-w-0 rounded-xl border border-slate-200 bg-paper px-4 py-3 pr-16 outline-none focus:border-eduBlue" placeholder="Create a password"/><button type="button" onClick={()=>setShowPassword(!showPassword)} className="absolute right-3 top-3 text-xs font-bold text-eduBlue">{showPassword?'Hide':'Show'}</button></div></label><label className="min-w-0 text-sm font-bold text-ink">Confirm password<input name="confirmPassword" required minLength={6} type={showPassword?'text':'password'} className="mt-2 w-full rounded-xl border border-slate-200 bg-paper px-4 py-3 outline-none focus:border-eduBlue" placeholder="Repeat password"/></label></div><div className="flex items-start gap-3 rounded-2xl bg-slate-50 p-4"><ShieldCheck className="mt-0.5 shrink-0 text-emerald-600" size={18}/><p className="text-xs leading-5 text-slate-500">Your account is created for free. Every account receives a unique EDUWILLS account ID that can be used in its account link, helping identify the correct account even when a name or username is unavailable.</p></div>{message&&<p className="rounded-xl bg-red-50 px-4 py-3 text-sm leading-5 text-red-600">{message}</p>}<button disabled={saving} type="submit" className="w-full rounded-xl bg-eduBlue px-5 py-3.5 font-black text-white shadow-lg shadow-blue-100 disabled:opacity-60">{saving?'Creating account…':'Create free account'}</button><p className="text-center text-sm text-slate-500">Already have an account? <a href={`${BASE}/login/`} className="font-bold text-eduBlue">Log in</a></p></form></div></section></div></div></main>}
