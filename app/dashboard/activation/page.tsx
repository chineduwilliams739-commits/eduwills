'use client';

import { useState } from 'react';
import { ArrowLeft, CheckCircle2, Copy, KeyRound, Upload, WalletCards, MessageCircle } from 'lucide-react';

const account = { number: '8129002773', bank: 'Palmpay', holder: 'Helen Umunnakwe' };

export default function ActivationPage() {
  const [copied, setCopied] = useState(false);
  const [file, setFile] = useState('');
  const copy = async () => { await navigator.clipboard?.writeText(account.number); setCopied(true); setTimeout(() => setCopied(false), 1800); };
  const whatsapp = `https://wa.me/?text=${encodeURIComponent('EDUWILLS activation proof submission. I have made my activation payment and will provide the receipt here.')}`;
  return (
    <main className="min-h-screen bg-paper px-5 py-6 sm:px-8"><div className="mx-auto max-w-6xl"><a href="/dashboard" className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Back to dashboard</a>
      <div className="mt-8 grid gap-6 lg:grid-cols-[1fr_390px]">
        <section className="rounded-[2rem] border border-slate-200 bg-white p-7 shadow-soft sm:p-9"><div className="inline-flex items-center gap-2 rounded-full bg-blue-50 px-3 py-1.5 text-xs font-black uppercase tracking-wider text-eduBlue"><WalletCards size={14}/> Activation</div><h1 className="mt-5 text-3xl font-black tracking-tight text-ink">Activate your EDUWILLS account</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">Registration is free. To unlock your learning features, make the required payment and submit your receipt for verification. An admin-generated WilliToken will activate your account for the approved duration.</p>
          <div className="mt-8 rounded-3xl bg-ink p-6 text-white"><div className="text-xs font-black uppercase tracking-[.18em] text-cyan-200">Payment account</div><div className="mt-5 space-y-4"><div><div className="text-xs text-slate-400">Account holder</div><div className="font-black">{account.holder}</div></div><div><div className="text-xs text-slate-400">Bank</div><div className="font-black">{account.bank}</div></div><div className="flex items-end justify-between gap-4"><div><div className="text-xs text-slate-400">Account number</div><div className="text-2xl font-black tracking-wider">{account.number}</div></div><button onClick={copy} className="rounded-xl bg-white/10 px-3 py-2 text-xs font-bold">{copied ? 'Copied' : <span className="inline-flex items-center gap-1"><Copy size={13}/> Copy</span>}</button></div></div></div>
          <div className="mt-6 grid gap-4 sm:grid-cols-3">{[['1','Make payment','Pay the activation amount into the account above.'],['2','Keep your receipt','Take a clear screenshot/photo of your payment receipt.'],['3','Submit proof','Upload the receipt and provide a WhatsApp contact for verification.']].map(([n,t,d])=><div key={n} className="rounded-2xl border border-slate-200 p-4"><div className="grid h-8 w-8 place-items-center rounded-lg bg-blue-50 text-xs font-black text-eduBlue">{n}</div><h3 className="mt-4 text-sm font-black">{t}</h3><p className="mt-1 text-xs leading-5 text-slate-500">{d}</p></div>)}</div>
          <div className="mt-7 rounded-3xl border border-slate-200 p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-blue-50 text-eduBlue"><Upload size={18}/></div><div><h2 className="font-black">Submit payment proof</h2><p className="text-xs text-slate-500">Receipt image or PDF</p></div></div><input type="file" accept="image/*,.pdf" onChange={(e) => setFile(e.target.files?.[0]?.name ?? '')} className="mt-5 block w-full rounded-xl border border-dashed border-slate-300 bg-paper p-4 text-sm"/><div className="mt-3 text-xs text-slate-500">{file ? `Selected: ${file}` : 'Choose a clear receipt.'}</div><button className="mt-5 w-full rounded-xl bg-eduBlue px-5 py-3.5 font-black text-white">Submit proof for review</button><p className="mt-3 text-center text-xs leading-5 text-slate-400">On the static preview, the upload is selected locally. Production submission will securely store the receipt and notify the admin.</p></div>
        </section>
        <aside className="space-y-6"><div className="rounded-[2rem] border border-slate-200 bg-white p-7 shadow-soft"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-emerald-50 text-emerald-600"><KeyRound size={22}/></div><h2 className="mt-5 text-xl font-black">Have a WilliToken?</h2><p className="mt-2 text-sm leading-6 text-slate-500">Enter the token supplied by EDUWILLS admin to activate your account for the duration assigned to it.</p><input className="mt-5 w-full rounded-xl border border-slate-200 bg-paper px-4 py-3 font-mono uppercase tracking-widest outline-none focus:border-eduBlue" placeholder="AB12CD34EF"/><button className="mt-3 w-full rounded-xl bg-ink px-5 py-3.5 font-black text-white">Activate with WilliToken</button><div className="mt-4 flex gap-2 text-xs leading-5 text-slate-400"><CheckCircle2 size={15} className="mt-0.5 shrink-0"/> Tokens are generated by EDUWILLS admin and expire according to their assigned duration.</div></div>
        <div className="rounded-[2rem] border border-emerald-100 bg-emerald-50 p-7"><div className="flex items-center gap-2 text-sm font-black text-emerald-800"><MessageCircle size={18}/> WhatsApp requirement</div><p className="mt-3 text-sm leading-6 text-emerald-900/70">A working WhatsApp account is required for activation verification. The production system will notify the admin after a receipt is securely submitted.</p><a href={whatsapp} target="_blank" rel="noreferrer" className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-white px-4 py-3 text-sm font-black text-emerald-800 shadow-sm">Open WhatsApp</a></div></aside>
      </div></div>
    </main>
  );
}
