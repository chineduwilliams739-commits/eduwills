'use client';

import { ArrowLeft, Lock, UserRound } from 'lucide-react';

export default function PersonalPage() {
  return <main className="min-h-screen bg-paper px-5 py-6 sm:px-8"><div className="mx-auto max-w-4xl"><a href="/dashboard" className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Back to dashboard</a><div className="mt-8 rounded-[2rem] border border-slate-200 bg-white p-7 shadow-soft sm:p-10"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-ink text-white"><UserRound size={24}/></div><p className="mt-6 text-xs font-black uppercase tracking-[.2em] text-eduBlue">Personal</p><h1 className="mt-2 text-3xl font-black tracking-tight text-ink">Your personal space</h1><p className="mt-2 text-sm leading-6 text-slate-500">Your profile, account details and preferences will live here. Personal remains available even while learning features are locked.</p><div className="mt-8 grid gap-4 sm:grid-cols-2">{[['Profile details','Name, username and phone number'],['Security','Password and account recovery'],['Learning categories','Your selected learning categories'],['Notifications','Important account and activation notices']].map(([t,d])=><div key={t} className="rounded-2xl border border-slate-200 p-5"><div className="flex items-center gap-2 font-black"><Lock size={15} className="text-eduBlue"/>{t}</div><p className="mt-2 text-xs leading-5 text-slate-500">{d}</p></div>)}</div></div></div></main>;
}
