'use client';

import { useEffect, useState } from 'react';
import { ArrowLeft, ArrowRight, BookOpen, CheckCircle2, GraduationCap, Sparkles } from 'lucide-react';
const BASE='/eduwills';
export default function CategoryQuizTestPage(){const[category,setCategory]=useState('senior');const[mode,setMode]=useState('WAEC');const[subjects,setSubjects]=useState<string[]>([]);useEffect(()=>{const p=new URLSearchParams(window.location.search);setCategory(p.get('category')||'senior');setMode(p.get('mode')||'WAEC');setSubjects((p.get('subjects')||'').split('|').filter(Boolean))},[]);return <main className="min-h-screen bg-paper text-ink"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-4xl items-center justify-between px-5 py-4"><a href={`${BASE}/dashboard/exam/?level=${category}`} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2 text-sm font-black"><ArrowLeft size={17}/> Back to exam setup</a><span className="flex items-center gap-2 font-black"><BookOpen size={18}/> EDUWILLS</span></div></header><div className="mx-auto max-w-3xl px-5 py-10"><section className="rounded-[2rem] bg-ink p-7 text-white shadow-2xl sm:p-10"><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-[10px] font-black uppercase tracking-[.18em] text-cyan-200"><GraduationCap size={13}/> EXAM READY</span><h1 className="mt-4 text-3xl font-black sm:text-4xl">Your {mode} setup is ready.</h1><p className="mt-3 text-sm leading-6 text-slate-300">Review the selected subjects before opening Quiz Studio.</p></section><section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center gap-3"><CheckCircle2 className="text-emerald-600" size={22}/><div><p className="text-xs font-black uppercase tracking-wider text-slate-400">Selected subjects</p><p className="text-lg font-black">{subjects.length} subject{subjects.length===1?'':'s'}</p></div></div><div className="mt-5 flex flex-wrap gap-2">{subjects.map(s=><span key={s} className="rounded-full bg-slate-100 px-3 py-2 text-xs font-black">{s}</span>)}</div><div className="mt-7 rounded-2xl bg-cyan-50 p-4"><p className="text-sm font-black">Next step</p><p className="mt-1 text-xs leading-5 text-slate-600">Open the main Quiz Studio to continue with the selected examination context.</p></div><a href={`${BASE}/dashboard/quiz/`} className="mt-6 flex w-full items-center justify-center gap-2 rounded-2xl bg-gradient-to-r from-indigo-600 to-cyan-500 px-5 py-4 text-sm font-black text-white shadow-lg">Open Quiz Studio <Sparkles size={17}/><ArrowRight size={17}/></a></section></div></main>}
