'use client';
import { BookOpen, Clock3, LogOut, Menu, UserRound, WalletCards, X, Sparkles, ArrowRight, LockKeyhole, Bot, CheckCircle2 } from 'lucide-react';
import { useEffect, useState } from 'react';
import { onAuthStateChanged, signOut } from 'firebase/auth';
import { doc, getDoc } from 'firebase/firestore';
import { auth, db } from '@/lib/firebase';
const BASE='/eduwills';
const nav=[{name:'QUIZ',icon:Sparkles,href:`${BASE}/dashboard/`},{name:'HISTORY',icon:Clock3,href:`${BASE}/dashboard/history/`},{name:'ACTIVATION',icon:WalletCards,href:`${BASE}/dashboard/activation/`},{name:'EDUWILLS AI',icon:Bot,href:`${BASE}/dashboard/ai/`},{name:'PERSONAL',icon:UserRound,href:`${BASE}/dashboard/personal/`}];
function expiryMs(v:any){if(!v)return 0;if(typeof v.toMillis==='function')return v.toMillis();if(v.seconds)return v.seconds*1000;const n=Date.parse(String(v));return Number.isFinite(n)?n:0;}
export default function DashboardPage(){const [mobileOpen,setMobileOpen]=useState(false),[name,setName]=useState('Learner'),[activated,setActivated]=useState(false),[loading,setLoading]=useState(true),[expiry,setExpiry]=useState('');useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u){window.location.replace(`${BASE}/login/`);return;}try{const s=await getDoc(doc(db,'users',u.uid));if(!s.exists())return;const d=s.data();setName(d.fullName?.split(' ')[0]||'Learner');const ms=expiryMs(d.activationExpiresAt);setActivated(d.activated===true&&ms>Date.now());if(ms)setExpiry(new Date(ms).toLocaleDateString());}catch(e){console.error(e)}finally{setLoading(false)}}),[]);async function logout(){await signOut(auth);window.location.replace(`${BASE}/`)}const locked=(n:string)=>!activated&&(n==='QUIZ'||n==='HISTORY'||n==='EDUWILLS AI');return <main className="min-h-screen bg-paper pb-24 text-ink"><header className="sticky top-0 z-30 border-b border-slate-200/80 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 sm:px-8"><a href={`${BASE}/`} className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-ink text-white"><BookOpen size={20}/></div><div><div className="font-black">EDUWILLS</div><div className="hidden text-[9px] font-bold uppercase tracking-[.18em] text-slate-400 sm:block">Book Learner</div></div></a><button className="rounded-xl border border-slate-200 p-2 md:hidden" onClick={()=>setMobileOpen(!mobileOpen)}>{mobileOpen?<X size={20}/>:<Menu size={20}/>}</button><span className={`hidden rounded-full px-3 py-1.5 text-xs font-bold md:block ${activated?'bg-emerald-50 text-emerald-700':'bg-amber-50 text-amber-700'}`}>{activated?'Activated':'Not activated'}</span></div></header><div className="mx-auto max-w-7xl px-5 py-6 sm:px-8"><section className="rounded-[2rem] bg-ink p-7 text-white sm:p-10"><div className="flex flex-col justify-between gap-7 sm:flex-row sm:items-end"><div><div className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-[10px] font-black uppercase tracking-widest text-cyan-200"><Sparkles size={12}/> Book Learner</div><h1 className="mt-4 text-3xl font-black sm:text-4xl">Hello {name}, welcome back.</h1><p className="mt-2 max-w-xl text-sm leading-6 text-slate-300">{loading?'Checking your account…':activated?`Your account is active${expiry?` until ${expiry}`:''}. Your learning tools are unlocked.`:'Your account is registered. Activate it to unlock Quiz, History and EDUWILLS AI.'}</p></div>{!activated&&<a href={`${BASE}/dashboard/activation/`} className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-black text-ink">Activate account <ArrowRight size={17}/></a>}</div></section><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{nav.map(({name:n,icon:Icon,href})=>{const l=locked(n);return <a key={n} href={l?'#':href} onClick={e=>{if(l){e.preventDefault();alert('Activate your EDUWILLS account with a WilliToken to unlock this section.')}}} className={`rounded-2xl border p-5 shadow-sm ${l?'border-slate-200 bg-white/70 opacity-60':'border-slate-200 bg-white hover:-translate-y-0.5'}`}><div className="flex items-center justify-between"><span className="grid h-10 w-10 place-items-center rounded-xl bg-slate-100"><Icon size={19}/></span>{l?<LockKeyhole size={16}/>:<CheckCircle2 size={16} className="text-emerald-500"/>}</div><p className="mt-4 text-sm font-black">{n}</p><p className="mt-1 text-xs text-slate-400">{l?'Locked until activation':n==='QUIZ'?'Create a personalized quiz':n==='HISTORY'?'Review your results':n==='EDUWILLS AI'?'Chat with your study assistant':'Open section'}</p></a>})}</div></div><nav className="fixed bottom-0 left-0 right-0 z-50 border-t border-slate-200 bg-white/95 px-1 py-2 backdrop-blur"><div className="mx-auto flex max-w-3xl justify-between">{nav.map(({name:n,icon:Icon,href})=>{const l=locked(n);return <a key={n} href={l?'#':href} onClick={e=>{if(l){e.preventDefault();alert('Activate your EDUWILLS account with a WilliToken to unlock this section.')}}} className="flex min-w-0 flex-1 flex-col items-center gap-1 py-1.5 text-[9px] font-black text-slate-500"><span className="relative"><Icon size={19}/>{l&&<LockKeyhole size={8} className="absolute -right-2 -top-1 rounded-full bg-white"/>}</span><span>{n}</span></a>})}</div></nav></main>}
