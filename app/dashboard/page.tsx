'use client';
import { BookOpen, Clock3, LogOut, Menu, UserRound, WalletCards, X, Sparkles, ArrowRight, LockKeyhole, Bot } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';
import { useEffect, useState } from 'react';
const BASE='/eduwills';
const nav: {name:string; icon:LucideIcon; href:string; locked?:boolean}[]=[{name:'QUIZ',icon:Sparkles,href:`${BASE}/dashboard/`,locked:true},{name:'HISTORY',icon:Clock3,href:`${BASE}/dashboard/history/`,locked:true},{name:'ACTIVATION',icon:WalletCards,href:`${BASE}/dashboard/activation/`},{name:'EDUWILLS AI',icon:Bot,href:`${BASE}/dashboard/ai/`,locked:true},{name:'PERSONAL',icon:UserRound,href:`${BASE}/dashboard/personal/`}];
export default function DashboardPage(){
 const[mobileOpen,setMobileOpen]=useState(false); const[name,setName]=useState('Learner');
 useEffect(()=>{const username=localStorage.getItem('eduwills_current_user');const users=JSON.parse(localStorage.getItem('eduwills_users')||'[]');const user=users.find((u:{username:string})=>u.username===username);if(user?.fullName)setName(user.fullName.split(' ')[0]);},[]);
 return <main className="min-h-screen bg-paper pb-24 text-ink"><header className="sticky top-0 z-30 border-b border-slate-200/80 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 sm:px-8 lg:px-10"><a href={`${BASE}/`} className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-ink text-white"><BookOpen size={20}/></div><div><div className="font-black tracking-tight">EDUWILLS</div><div className="hidden text-[9px] font-bold uppercase tracking-[.18em] text-slate-400 sm:block">Book Learner</div></div></a><button className="rounded-xl border border-slate-200 p-2 md:hidden" onClick={()=>setMobileOpen(!mobileOpen)}>{mobileOpen?<X size={20}/>:<Menu size={20}/>}</button><div className="hidden items-center gap-4 md:flex"><span className="rounded-full bg-amber-50 px-3 py-1.5 text-xs font-bold text-amber-700">Not activated</span><div className="grid h-9 w-9 place-items-center rounded-full bg-ink text-sm font-black text-white">{name[0]}</div></div></div></header>
 <div className="mx-auto max-w-7xl px-5 py-6 sm:px-8 lg:px-10"><section className="rounded-[2rem] bg-ink p-7 text-white sm:p-10"><div className="flex flex-col justify-between gap-7 sm:flex-row sm:items-end"><div><div className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-[10px] font-black uppercase tracking-widest text-cyan-200"><Sparkles size={12}/> Book Learner</div><h1 className="mt-4 text-3xl font-black tracking-tight sm:text-4xl">Hello {name}, welcome back.</h1><p className="mt-2 max-w-xl text-sm leading-6 text-slate-300">Your EDUWILLS learning space is ready. Activate your account to unlock your full learning experience.</p></div><a href={`${BASE}/dashboard/activation/`} className="inline-flex items-center justify-center gap-2 rounded-xl bg-white px-5 py-3 text-sm font-black text-ink">Activate account <ArrowRight size={17}/></a></div></section>
 <div className="mt-7 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-bold text-slate-500">Your learning tools</p><p className="mt-1 text-xs text-slate-400">Use the navigation below to move between your five dedicated sections.</p></div></div>
 <aside className={`${mobileOpen?'fixed inset-x-5 top-20 z-40 block':'hidden'} w-auto rounded-2xl border border-slate-200 bg-white p-3 shadow-soft md:hidden`}><div className="space-y-1">{nav.map(({name,icon:Icon,href,locked})=><a key={name} href={href} onClick={()=>setMobileOpen(false)} className="flex items-center justify-between rounded-xl px-4 py-3 text-sm font-bold text-slate-600"><span className="flex items-center gap-3"><Icon size={18}/>{name}</span>{locked&&<LockKeyhole size={14} className="text-slate-300"/>}</a>)}</div><div className="my-3 border-t border-slate-100"/><a href={`${BASE}/`} className="flex items-center gap-3 rounded-xl px-4 py-3 text-sm font-bold text-slate-500"><LogOut size={18}/>Log out</a></aside>
 <nav className="fixed bottom-0 left-0 right-0 z-50 border-t border-slate-200 bg-white/95 px-1 py-2 backdrop-blur md:px-4"><div className="mx-auto flex max-w-3xl items-stretch justify-between">{nav.map(({name,icon:Icon,href,locked})=><a key={name} href={href} className="flex min-w-0 flex-1 flex-col items-center gap-1 rounded-xl px-1 py-1.5 text-[9px] font-black text-slate-500"><span className="relative grid h-7 w-7 place-items-center"><Icon size={19}/>{locked&&<LockKeyhole size={8} className="absolute -right-1 -top-1 rounded-full bg-white text-slate-400"/>}</span><span className="truncate">{name}</span></a>)}</div></nav></main>}
