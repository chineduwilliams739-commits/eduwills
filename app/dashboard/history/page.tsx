'use client';
import { ArrowLeft, BookOpen, CheckCircle2, Clock3, Trophy, XCircle } from 'lucide-react';
import { useEffect, useState } from 'react';
import { onAuthStateChanged } from 'firebase/auth';
import { collection, doc, getDoc, getDocs, query, where } from 'firebase/firestore';
import { auth, db } from '@/lib/firebase';
const BASE = '/eduwills';
function expiryMs(v: any) { if (!v) return 0; if (typeof v.toMillis === 'function') return v.toMillis(); if (v.seconds) return v.seconds * 1000; const n = Date.parse(String(v)); return Number.isFinite(n) ? n : 0; }
function elapsedText(total: number) { const m = Math.floor(total / 60).toString().padStart(2, '0'); const s = (total % 60).toString().padStart(2, '0'); return `${m}:${s}`; }
export default function HistoryPage() {
  const [active, setActive] = useState(false), [items, setItems] = useState<any[]>([]), [selected, setSelected] = useState<any | null>(null), [loading, setLoading] = useState(true);
  useEffect(() => onAuthStateChanged(auth, async (u) => { if (!u) { window.location.replace(`${BASE}/login/`); return; } try { const s = await getDoc(doc(db, 'users', u.uid)); const d = s.data() || {}; setActive(d.activated === true && expiryMs(d.activationExpiresAt) > Date.now()); const q = await getDocs(query(collection(db, 'quizHistory'), where('userId', '==', u.uid))); const list = q.docs.map((x) => ({ id: x.id, ...x.data() })).sort((a: any, b: any) => (b.createdAt?.seconds || 0) - (a.createdAt?.seconds || 0)); setItems(list); const id = new URLSearchParams(window.location.search).get('id'); if (id) setSelected(list.find((x: any) => x.id === id) || null); } catch (e) { console.error(e); } finally { setLoading(false); } }), []);
  if (loading) return <main className="grid min-h-screen place-items-center bg-paper"><p className="font-black">Loading your history…</p></main>;
  if (!active) return <main className="min-h-screen bg-paper px-5 py-8"><div className="mx-auto max-w-4xl"><a href={`${BASE}/dashboard/`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Dashboard</a><div className="mt-8 rounded-[2rem] bg-white p-10 text-center shadow-soft"><p className="font-black text-amber-800">History is locked until your account is activated.</p><a href={`${BASE}/dashboard/activation/`} className="mt-4 inline-flex rounded-xl bg-eduBlue px-4 py-2.5 font-black text-white">Activate account</a></div></div></main>;
  if (selected) { const qs = selected.questionsData || []; const answers = selected.answers || []; const wrong = qs.map((q: any, i: number) => ({ q, i })).filter(({ q, i }: any) => answers[i] !== q.answer); const pct = selected.percentage ?? Math.round((selected.score || 0) / Math.max(1, selected.total || qs.length) * 100); return <main className="min-h-screen bg-paper px-5 py-8"><div className="mx-auto max-w-4xl"><button onClick={() => { setSelected(null); history.replaceState({}, '', `${BASE}/dashboard/history/`); }} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Back to History</button><section className="mt-6 rounded-[2rem] bg-gradient-to-br from-slate-950 via-indigo-950 to-cyan-900 p-8 text-white shadow-xl"><p className="text-xs font-black uppercase tracking-[.2em] text-cyan-200">Test Overview</p><h1 className="mt-3 text-3xl font-black">{pct}%</h1><p className="mt-1 text-lg font-bold text-slate-300">{selected.score || 0}/{selected.total || qs.length} correct</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-white/10 p-4"><b>📚 Books</b><br/>{(selected.books || []).map((b: any) => `${b.title} — ${b.author}`).join(', ')}</div><div className="rounded-2xl bg-white/10 p-4"><b>🎯 Difficulty</b><br/>{selected.difficulty || 'Mixed'}</div><div className="rounded-2xl bg-white/10 p-4"><b>⏱ Time allocated</b><br/>{selected.duration ? `${selected.duration} minutes` : 'No time limit'}</div><div className="rounded-2xl bg-white/10 p-4"><b>⌛ Time elapsed</b><br/>{elapsedText(selected.elapsedSeconds || 0)}</div></div></section><section className="mt-6 rounded-[2rem] bg-white p-7 shadow-soft"><h2 className="text-xl font-black">Correction review</h2><div className="mt-5 space-y-4">{qs.length ? qs.map((q: any, i: number) => { const ok = answers[i] === q.answer; return <article key={i} className="rounded-2xl border border-slate-200 p-5"><p className="text-xs font-black uppercase text-slate-400">Question {i + 1}</p><p className="mt-2 font-black leading-7">{q.question}</p>{ok ? <div className="mt-3 rounded-2xl border border-emerald-200 bg-emerald-50 p-4"><p className="font-black text-emerald-700">✓ Correct</p><p className="mt-1 text-emerald-900">{q.options[q.answer]}</p></div> : <><div className="mt-3 rounded-2xl border border-red-200 bg-red-50 p-4"><p className="font-black text-red-700">✕ Your answer</p><p className="mt-1 text-red-900">{answers[i] == null ? 'Not answered' : q.options[answers[i]]}</p></div><div className="mt-3 rounded-2xl border border-emerald-200 bg-emerald-50 p-4"><p className="font-black text-emerald-700">✓ Correct answer</p><p className="mt-1 text-emerald-900">{q.options[q.answer]}</p></div></>}</article>; }) : <p className="text-sm text-slate-500">Detailed corrections were not stored for this older test.</p>}</div></section></div></main>; }
  return <main className="min-h-screen bg-paper px-5 py-8 sm:px-8"><div className="mx-auto max-w-5xl"><a href={`${BASE}/dashboard/`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Dashboard</a><section className="mt-8 rounded-[2rem] border border-slate-200 bg-white p-7 shadow-soft sm:p-10"><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-eduBlue"><Clock3/></div><div><p className="text-xs font-black uppercase tracking-[.2em] text-eduBlue">Book Learner</p><h1 className="text-3xl font-black text-ink">Quiz History</h1></div></div>{items.length === 0 ? <div className="mt-8 rounded-2xl border border-dashed border-slate-200 p-10 text-center"><Trophy className="mx-auto text-slate-300"/><p className="mt-3 font-black">No quizzes taken yet</p><a href={`${BASE}/dashboard/quiz/`} className="mt-4 inline-flex rounded-xl bg-ink px-4 py-2.5 text-sm font-black text-white">Take your first quiz</a></div> : <div className="mt-8 grid gap-3">{items.map((x) => { const pct = x.percentage ?? (x.score != null ? Math.round((x.score / Math.max(1, x.total || x.questions || 1)) * 100) : 0); return <button key={x.id} onClick={() => { setSelected(x); history.pushState({}, '', `${BASE}/dashboard/history/?id=${x.id}`); }} className="w-full rounded-2xl border border-slate-200 bg-white p-5 text-left transition hover:-translate-y-0.5 hover:border-eduBlue hover:shadow-md"><div className="flex items-start justify-between gap-4"><div><p className="flex items-center gap-2 font-black"><BookOpen size={16}/>{(x.books || []).map((b: any) => b.title).join(', ') || 'Quiz'}</p><p className="mt-1 text-xs text-slate-400">{x.difficulty || 'Mixed'} · {x.total || x.questions || 0} questions · {x.duration ? `${x.duration} min` : 'No limit'}</p></div><span className={`rounded-full px-3 py-1 text-xs font-black ${pct >= 70 ? 'bg-emerald-50 text-emerald-700' : 'bg-amber-50 text-amber-700'}`}>{pct}%</span></div><div className="mt-3 text-xs font-bold text-slate-400">{x.elapsedSeconds ? `Time: ${elapsedText(x.elapsedSeconds)}` : 'Tap to view Test Overview →'}</div></button>; })}</div>}</section></div></main>;
}
