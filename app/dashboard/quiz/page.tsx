'use client';
import { useEffect,useMemo,useState } from 'react';
import { ArrowLeft,BookOpen,Sparkles,Clock3,CheckCircle2 } from 'lucide-react';
import { onAuthStateChanged } from 'firebase/auth';
import { addDoc,collection,doc,getDoc,getDocs,query,serverTimestamp,where } from 'firebase/firestore';
import { auth,db } from '@/lib/firebase';
const BASE='/eduwills';
function expiryMs(v:any){if(!v)return 0;if(typeof v.toMillis==='function')return v.toMillis();if(v.seconds)return v.seconds*1000;const n=Date.parse(String(v));return Number.isFinite(n)?n:0;}
type Book={id:string;slot:number;title:string;author:string};
export default function QuizPage(){const [active,setActive]=useState(false),[books,setBooks]=useState<Book[]>([]),[selected,setSelected]=useState<string[]>([]),[questions,setQuestions]=useState(10),[duration,setDuration]=useState('20'),[difficulty,setDifficulty]=useState('Mixed'),[instructions,setInstructions]=useState(''),[message,setMessage]=useState(''),[loading,setLoading]=useState(true),[started,setStarted]=useState(false),[saved,setSaved]=useState(false);
 useEffect(()=>onAuthStateChanged(auth,async u=>{if(!u){window.location.replace(`${BASE}/login/`);return;}try{const us=await getDoc(doc(db,'users',u.uid));const d=us.data()||{};setActive(d.activated===true&&expiryMs(d.activationExpiresAt)>Date.now());const bs=await getDocs(query(collection(db,'bookSlots'),where('userId','==',u.uid)));setBooks(bs.docs.map(x=>({id:x.id,...x.data()} as Book)).sort((a,b)=>a.slot-b.slot));}catch(e){console.error(e);setMessage('Could not load your quiz library.')}finally{setLoading(false)}}),[]);
 const selectedBooks=useMemo(()=>books.filter(b=>selected.includes(b.id)),[books,selected]);
 async function createQuiz(){setMessage('');if(!selectedBooks.length){setMessage('Select at least one book.');return;}if(instructions.length>100){setMessage('Instructions must be 100 characters or fewer.');return;}try{const uid=auth.currentUser!.uid;await addDoc(collection(db,'quizHistory'),{userId:uid,books:selectedBooks.map(b=>({title:b.title,author:b.author})),questions,duration:duration==='none'?null:Number(duration),difficulty,instructions,status:'generated',createdAt:serverTimestamp()});setSaved(true);setStarted(true);}catch(e){console.error(e);setMessage('Could not create the quiz. Check your connection and try again.')}}
 if(!active&&!loading)return <main className="min-h-screen bg-paper px-5 py-8"><div className="mx-auto max-w-3xl"><a href={`${BASE}/dashboard/`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Dashboard</a><section className="mt-8 rounded-[2rem] bg-white p-10 text-center shadow-soft"><Sparkles className="mx-auto text-slate-300" size={44}/><h1 className="mt-5 text-3xl font-black">Quiz is locked</h1><p className="mt-2 text-slate-500">Activate your account with a WilliToken to unlock the Book Learner quiz generator.</p><a href={`${BASE}/dashboard/activation/`} className="mt-6 inline-flex rounded-xl bg-eduBlue px-5 py-3 font-black text-white">Activate account</a></section></div></main>;
 return <main className="min-h-screen bg-paper text-ink"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-5xl items-center justify-between px-5 py-4"><a href={`${BASE}/dashboard/`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Dashboard</a><div className="flex items-center gap-2 font-black"><Sparkles className="text-eduBlue"/> QUIZ</div></div></header><div className="mx-auto max-w-5xl px-5 py-8"><section className="rounded-[2rem] bg-ink p-7 text-white sm:p-10"><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-black uppercase tracking-wider text-cyan-200"><Sparkles size={13}/> Book Learner</span><h1 className="mt-5 text-3xl font-black sm:text-4xl">Create your personalized quiz</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Choose your saved books, set the quiz rules, and tell EDUWILLS AI exactly what to focus on.</p></section><section className="mt-7 rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex items-center gap-3"><BookOpen className="text-eduBlue"/><h2 className="text-xl font-black">1. Select book/s</h2></div>{loading?<p className="mt-5 text-sm text-slate-500">Loading your books…</p>:books.length===0?<p className="mt-5 rounded-2xl bg-slate-50 p-5 text-sm text-slate-500">You haven't saved any books yet. Add your books from EDUWILLS AI → My Books.</p>:<div className="mt-5 grid gap-3 sm:grid-cols-2">{books.map(b=><button key={b.id} onClick={()=>setSelected(s=>s.includes(b.id)?s.filter(x=>x!==b.id):[...s,b.id])} className={`rounded-2xl border p-4 text-left ${selected.includes(b.id)?'border-eduBlue bg-blue-50':'border-slate-200'}`}><p className="text-[10px] font-black uppercase tracking-wider text-slate-400">Slot {b.slot}</p><p className="mt-1 font-black">{selected.includes(b.id)?'✓ ':''}{b.title}</p><p className="text-sm text-slate-500">{b.author}</p></button>)}</div>}</section><section className="mt-5 rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><h2 className="text-xl font-black">2. Quiz settings</h2><div className="mt-5 grid gap-5 md:grid-cols-3"><label className="text-sm font-black">Questions <span className="font-normal text-slate-400">1–100</span><input type="number" min="1" max="100" value={questions} onChange={e=>setQuestions(Math.min(100,Math.max(1,Number(e.target.value)||1)))} className="mt-2 w-full rounded-xl border border-slate-200 bg-paper px-4 py-3"/></label><label className="text-sm font-black">Duration<select value={duration} onChange={e=>setDuration(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3"><option value="10">10 minutes</option><option value="20">20 minutes</option><option value="30">30 minutes</option><option value="45">45 minutes</option><option value="60">60 minutes</option><option value="none">No time limit</option></select></label><label className="text-sm font-black">Difficulty<select value={difficulty} onChange={e=>setDifficulty(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3"><option>Easy</option><option>Medium</option><option>Hard</option><option>Mixed</option></select></label></div><label className="mt-6 block text-sm font-black">Instructions for EDUWILLS AI <span className="font-normal text-slate-400">(max 100 characters)</span><textarea maxLength={100} value={instructions} onChange={e=>setInstructions(e.target.value)} className="mt-2 min-h-24 w-full rounded-xl border border-slate-200 bg-paper px-4 py-3" placeholder="e.g. Focus on chapter 2 and questions about Okonkwo."/><span className="mt-1 block text-right text-xs text-slate-400">{instructions.length}/100</span></label>{message&&<p className="mt-4 rounded-xl bg-amber-50 p-3 text-sm font-bold text-amber-800">{message}</p>}<button onClick={createQuiz} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-eduBlue px-5 py-4 font-black text-white"><Sparkles size={18}/> Generate Quiz</button></section>{started&&<section className="mt-5 rounded-3xl border border-emerald-100 bg-white p-7 text-center"><CheckCircle2 className="mx-auto text-emerald-600" size={42}/><h2 className="mt-3 text-2xl font-black">Quiz setup saved</h2><p className="mt-2 text-sm text-slate-500">{selectedBooks.length} book{selectedBooks.length>1?'s':''} · {questions} questions · {difficulty} · {duration==='none'?'No time limit':`${duration} minutes`}</p><p className="mt-3 text-xs text-slate-400">The AI generation layer can now use this saved configuration.</p><a href={`${BASE}/dashboard/history/`} className="mt-5 inline-flex items-center gap-2 rounded-xl border border-slate-200 px-5 py-3 text-sm font-black"><Clock3 size={16}/> View history</a></section>)}</div></main>}
