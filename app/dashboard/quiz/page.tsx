'use client';
import { useState } from 'react';
import { ArrowLeft, BookOpen, Search, Sparkles, CheckCircle2 } from 'lucide-react';
const BASE='/eduwills';
const samples=[
 {book:'Things Fall Apart',author:'Chinua Achebe',questions:['Who is the protagonist of Things Fall Apart?','What is the name of Okonkwo’s eldest son?','Which village is central to the story?'],answers:['Okonkwo','Nwoye','Umuofia']},
 {book:'The Lion and the Jewel',author:'Wole Soyinka',questions:['Who is the school teacher in the play?','Who is the village beauty?','Who is the older village man?'],answers:['Lakunle','Sidi','Baroka']}
];
export default function QuizPage(){const [query,setQuery]=useState('');const [book,setBook]=useState<typeof samples[number]|null>(null);const [started,setStarted]=useState(false);const [index,setIndex]=useState(0);const [answer,setAnswer]=useState('');const [score,setScore]=useState(0);const [done,setDone]=useState(false);
 const findBook=()=>{const found=samples.find(x=>x.book.toLowerCase().includes(query.trim().toLowerCase())); if(found)setBook(found); else if(query.trim())setBook({book:query.trim(),author:'Author lookup will be provided by EDUWILLS AI',questions:['What is the central theme of this book?','Who is the main character?','What important conflict drives the story?'],answers:['Theme-based response','Main character','Central conflict']});};
 const submit=()=>{if(!book)return; if(answer.trim().toLowerCase()===book.answers[index].toLowerCase())setScore(s=>s+1); if(index+1>=book.questions.length)setDone(true); else {setIndex(i=>i+1);setAnswer('')}};
 const reset=()=>{setStarted(false);setDone(false);setIndex(0);setScore(0);setAnswer('')};
 return <main className="min-h-screen bg-paper text-ink"><header className="border-b border-slate-200 bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-5 py-4"><a href={`${BASE}/dashboard/`} className="inline-flex items-center gap-2 text-sm font-bold text-slate-600"><ArrowLeft size={17}/> Dashboard</a><div className="flex items-center gap-2 font-black"><Sparkles className="text-eduBlue"/> QUIZ</div></div></header><div className="mx-auto max-w-5xl px-5 py-8"><section className="rounded-[2rem] bg-ink p-7 text-white sm:p-10"><span className="text-xs font-black uppercase tracking-[.2em] text-cyan-200">Book Quiz Generator</span><h1 className="mt-4 text-3xl font-black sm:text-4xl">Search a book. Build a quiz.</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">Search a book title and EDUWILLS will identify the author and prepare questions. The preview includes sample books while the full AI book engine is being connected.</p><div className="mt-7 flex gap-2"><input value={query} onChange={e=>setQuery(e.target.value)} onKeyDown={e=>e.key==='Enter'&&findBook()} className="min-w-0 flex-1 rounded-xl bg-white px-4 py-3 text-sm text-ink outline-none" placeholder="Search a book e.g. Things Fall Apart"/><button onClick={findBook} className="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-eduBlue text-white"><Search size={18}/></button></div></section>{book&&!started&&!done&&<section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-soft"><div className="flex items-start gap-4"><div className="grid h-12 w-12 shrink-0 place-items-center rounded-xl bg-blue-50 text-eduBlue"><BookOpen/></div><div><h2 className="text-xl font-black">{book.book}</h2><p className="mt-1 text-sm text-slate-500">Author: <strong>{book.author}</strong></p><p className="mt-3 text-sm text-slate-500">3-question preview quiz</p></div></div><button onClick={()=>setStarted(true)} className="mt-6 rounded-xl bg-ink px-5 py-3 text-sm font-black text-white">Start quiz</button></section>}{book&&started&&!done&&<section className="mt-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-soft sm:p-8"><div className="flex justify-between text-xs font-black text-slate-400"><span>Question {index+1} of {book.questions.length}</span><span>Score {score}</span></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-eduBlue transition-all" style={{width:`${((index+1)/book.questions.length)*100}%`}}/></div><h2 className="mt-8 text-2xl font-black">{book.questions[index]}</h2><p className="mt-2 text-sm text-slate-500">Type your answer below.</p><input value={answer} onChange={e=>setAnswer(e.target.value)} onKeyDown={e=>e.key==='Enter'&&submit()} className="mt-6 w-full rounded-xl border border-slate-200 bg-paper px-4 py-3 outline-none focus:border-eduBlue" placeholder="Your answer"/><button onClick={submit} disabled={!answer.trim()} className="mt-4 rounded-xl bg-eduBlue px-5 py-3 text-sm font-black text-white disabled:opacity-40">Submit answer</button></section>}{done&&<section className="mt-6 rounded-3xl border border-emerald-100 bg-white p-8 text-center shadow-soft"><CheckCircle2 className="mx-auto text-emerald-600" size={44}/><h2 className="mt-4 text-2xl font-black">Quiz complete!</h2><p className="mt-2 text-slate-500">You scored {score} out of {book?.questions.length}.</p><div className="mt-6 flex justify-center gap-3"><button onClick={reset} className="rounded-xl bg-ink px-5 py-3 text-sm font-black text-white">Try again</button><a href={`${BASE}/dashboard/history/`} className="rounded-xl border border-slate-200 px-5 py-3 text-sm font-black">View history</a></div></section>}</div></main>;
}
