import {NextRequest,NextResponse} from 'next/server';

type Book={title:string;author:string;content?:string;excerpt?:string;summary?:string;notes?:string};
type Source={title:string;text:string;url?:string};
type Question={question:string;options:string[];answer:number;explanation?:string};
const clean=(s:any)=>String(s??'').replace(/\s+/g,' ').trim();
const norm=(s:string)=>s.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9 ]/g,' ').replace(/\s+/g,' ').trim();
const fp=(s:string)=>norm(s).replace(/\b(the|a|an|what|which|who|how|why|did|does|is|was|were|of|in|on|to|and|for|from|about|according)\b/g,'').replace(/\s+/g,' ').trim();
const similar=(a:string,b:string)=>{const x=new Set(fp(a).split(' ').filter(Boolean)),y=new Set(fp(b).split(' ').filter(Boolean));if(!x.size||!y.size)return false;const hit=[...x].filter(v=>y.has(v)).length;return hit/Math.max(1,Math.min(x.size,y.size))>=0.84};
const authorMatches=(a:string,b:string)=>{const x=norm(a),y=norm(b);if(!x||!y)return false;if(x===y||x.includes(y)||y.includes(x))return true;const xt=new Set(x.split(' ').filter(Boolean)),yt=new Set(y.split(' ').filter(Boolean));return [...xt].filter(v=>yt.has(v)).length>=Math.min(2,xt.size,yt.size)};
const titleMatches=(a:string,b:string)=>{const x=fp(a),y=fp(b);if(!x||!y)return false;if(x===y)return true;const xt=new Set(x.split(' ').filter(Boolean)),yt=new Set(y.split(' ').filter(Boolean));const hit=[...xt].filter(v=>yt.has(v)).length;return hit/Math.max(1,Math.min(xt.size,yt.size))>=0.75};
const curated:Record<string,string[]>= {
 'the lekki headmaster':['The Lekki Headmaster was written by Kabir Alabi Garba.','The story follows Bepo Adewale, a dedicated headmaster at Stardom Schools in Lekki, Lagos.','The novel explores education, integrity, service, migration pressure and the japa phenomenon.','Bepo faces pressure to relocate to the United Kingdom but remains committed to his students and school.','The novel was published by Winepress Publishing in 2023.','JAMB selected The Lekki Headmaster as the general reading text for the 2025 and 2026 UTME Use of English.'],
 'sanya':['Sànyà is the 2022 debut novel by Nigerian author Oyin Olugbile, published by Masobe Books.','Sànyà is a mythological fantasy novel that reimagines Yoruba mythology, particularly the story and legacy of Sango, through a female protagonist.','The protagonist Sànyà is associated with a prophecy of a mighty warrior and leader whose life will have an unprecedented impact on her world.','Sànyà has a brother named Dada, who is physically weak but highly intelligent and has the gift of seeing into the future.','Sànyà leaves home after an unspeakable tragedy and later discovers that her powers are linked to a dark prophecy.','The novel explores family, love, identity, ancestry, power, Yoruba mythology, gods, Orisas and sorcerers.','Sànyà won the 2025 Nigeria Prize for Literature.','The official author and publisher descriptions identify Sànyà as a work of culture, fantasy and mythology.'],
 'scars':['SCARS: Nigeria’s Journey and the Boko Haram Conundrum chronicles the impact of Boko Haram on Nigeria and draws on Lucky Irabor’s first-hand military command experience in North East Nigeria.','SCARS is arranged in three parts: Part One, The Ghost that Lives with Us; Part Two, The Boko Haram Conundrum; and Part Three, Eyes Set on Tomorrow. It contains fourteen chapters and a concluding note.','Chapter 1, The Context, uses frontline experience and the problems of independent Nigeria to argue that a military-centric security response should be complemented by non-military elements of state power.','Chapter 2, When the Shield Becomes the Sword: Nigeria’s Security Dilemma, examines Nigeria’s political and socio-economic history, including the fall of the First Republic and the civil war, and connects rancorous politics with insecurity.','Chapter 3, The Aftermath: Echoes of Uncertainty, examines the aftermath of the First Republic and civil war and structural weaknesses that contribute to insecurity.','Chapter 4, Where Borders Breathe: The Pulse of North Eastern Nigeria, examines the geography, history, power structures, socio-economic conditions, culture, religion and communication systems of the region where Boko Haram activity is concentrated.','Chapter 5, Brief Overview of Boko Haram, covers the group’s background, origin, organization, ideology and early leadership profile.','Chapter 6, The Irony of Boko Haram, presents Boko Haram as an evil that harms people across socio-cultural, political and religious boundaries.','Chapter 7, The Beastly Boko Haram, documents violent killings, destruction, improvised explosive device attacks, targeted killings and school abductions.','Chapter 8, The Deeper Scars of the Boko Haram War, focuses on hardship, displacement, deaths, injuries, emotional trauma and suffering, especially in North East Nigeria.','Chapter 9, The Complexity of Response to Boko Haram, examines the federal government and military response and the difficulty of responding to an asymmetric and volatile security environment.','Chapter 10, The Political Dynamics of Boko Haram Crisis and Nigeria’s Sovereignty, examines political dynamics, sovereignty and negative media narratives around the conflict.','Chapter 11, General Outlook of the Security Landscape, looks at the future security environment and the social, economic and political damage caused by insecurity.','Chapter 12, Drivers of Extremist Activities, examines political intricacies and setbacks that can contribute to terrorism and extremism.','Chapter 13, Learning the Language of Peace, explores peaceful and innovative approaches to reducing terrorism and resolving conflict.','Chapter 14, Nigeria: The Peace we have yet to Try, advocates whole-of-government and whole-of-society approaches, psychological re-orientation, security-sector reforms, national reconciliation, judicial reforms, good governance and constitutional review.','A central idea in SCARS is that Nigeria’s security challenge cannot be solved by military force alone; broader political, social, institutional and psychological responses are required.','The book examines Boko Haram through both frontline military experience and wider reflections on Nigeria’s history, governance, insecurity, extremism and national cohesion.','The official author site describes the book as a catalogue of facts about Boko Haram drawn from Irabor’s first-hand experience during military operations in North East Nigeria.','The book’s concluding outlook is optimistic: Nigerians can collectively transform the nation’s scars into victories.']
};
const trusted:Record<string,string[]>= {
 'sanya':['https://www.oyinolugbile.com/books','https://masobebooks.com/ng/book/sanya/','https://virtuall.nln.gov.ng/resource/NLN-XAHR2EOY1LSD4Y1N0','https://www.thenigeriaprizes.org/wp-content/uploads/2025/10/2025-NIGERIA-PRIZE-FOR-LITERATURE-FINAL-REPORT.pdf'],
 'scars':['https://luckyirabor.com/scars/','https://www.premiumtimesng.com/entertainment/828546-book-review-inside-lucky-irabors-scars-nigerias-journey-and-the-boko-haram-conundrum.html','https://lifestyle.thecable.ng/book-review-scars-nigerias-journey-and-the-boko-haram-conundrum/']
};
function curatedKey(title:string){const k=norm(title);if(k==='sanya'||k.startsWith('sanya '))return 'sanya';if(k==='scars'||k.startsWith('scars '))return 'scars';if(k.includes('lekki headmaster'))return 'the lekki headmaster';return k}
async function json(url:string,ms=5000){const ctl=new AbortController(),t=setTimeout(()=>ctl.abort(),ms);try{const r=await fetch(url,{cache:'no-store',headers:{Accept:'application/json'},signal:ctl.signal});if(!r.ok)throw Error(String(r.status));return await r.json()}finally{clearTimeout(t)}}
function add(out:Source[],title:string,text:any,url?:string){const t=clean(text);if(t)out.push({title:clean(title)||'Book source',text:t.slice(0,12000),url})}
async function researchBook(book:Book){const out:Source[]=[];const title=clean(book.title),author=clean(book.author),t=encodeURIComponent(title),a=encodeURIComponent(author),key=curatedKey(title);
 for(const f of curated[key]||[])add(out,'EDUWILLS verified book-content catalogue',f);
 const supplied=[book.content,book.excerpt,book.summary,book.notes].map(clean).filter(Boolean).join('\n\n');
 if(supplied)add(out,'Saved book content supplied to EDUWILLS',`The following material belongs to the saved book ${title} by ${author}. It is the primary content source for quiz questions:\n${supplied}`,undefined);
 const urls:[string,string][]= [
  ['Google Books',`https://www.googleapis.com/books/v1/volumes?q=intitle:${t}+inauthor:${a}&maxResults=40`],
  ['Open Library',`https://openlibrary.org/search.json?title=${t}&author=${a}&limit=50`],
  ['Wikipedia',`https://en.wikipedia.org/api/rest_v1/page/summary/${encodeURIComponent(title.replace(/ /g,'_'))}`],
  ['Wikipedia search',`https://en.wikipedia.org/w/api.php?action=query&format=json&origin=*&prop=extracts&exintro=1&explaintext=1&titles=${t}`],
  ['Wikidata',`https://www.wikidata.org/w/api.php?action=wbsearchentities&search=${encodeURIComponent(title+' '+author)}&language=en&format=json&limit=6&origin=*`],
  ['Internet Archive',`https://archive.org/advancedsearch.php?q=title%3A%22${encodeURIComponent(title)}%22&fl%5B%5D=title&fl%5B%5D=description&fl%5B%5D=creator&rows=10&output=json`]
 ];
 const results=await Promise.allSettled(urls.map(x=>json(x[1],5000)));
 for(let i=0;i<results.length;i++){const r=results[i];if(r.status!=='fulfilled')continue;const d:any=r.value;
  if(i===0)for(const x of d.items||[]){const v=x.volumeInfo||{};const returnedTitle=clean(v.title);const returnedAuthors=Array.isArray(v.authors)?v.authors.join(' '):'';if(!titleMatches(returnedTitle,title)|| (returnedAuthors&&!authorMatches(returnedAuthors,author)))continue;add(out,v.title||title,v.description,x.selfLink);if(v.publishedDate)add(out,`${v.title||title} publication`,`Published ${v.publishedDate}. Publisher: ${v.publisher||'unknown'}.`,x.selfLink)}
  if(i===1)for(const x of d.docs||[]){const returnedTitle=clean(x.title||'');const returnedAuthors=Array.isArray(x.author_name)?x.author_name.join(' '):'';if(!titleMatches(returnedTitle,title)|| (returnedAuthors&&!authorMatches(returnedAuthors,author)))continue;add(out,x.title||title,(x.first_sentence||[]).join(' '),`https://openlibrary.org/works/${String(x.key||'').replace('/works/','')}`);if(x.subject)add(out,`${x.title||title} subjects`,(x.subject||[]).slice(0,100).join(', '))}
  if(i===2){const returnedTitle=clean(d.title||'');if(titleMatches(returnedTitle,title)&&norm(title)!=='sanya')add(out,d.title||title,d.extract,`https://en.wikipedia.org/wiki/${encodeURIComponent(title.replace(/ /g,'_'))}`)}
  if(i===3)for(const p of Object.values(d.query?.pages||{}) as any[]){const pt=clean(p.title||'');if(!titleMatches(pt,title))continue;if(norm(title)==='sanya'&&!authorMatches(p.extract||'',author))continue;add(out,p.title||title,p.extract,`https://en.wikipedia.org/wiki/${encodeURIComponent(String(p.title||title).replace(/ /g,'_'))}`)}
  if(i===4)for(const x of d.search||[]){const label=clean(x.label||''),desc=clean(x.description||'');if(!titleMatches(label,title)&&!authorMatches(label,author))continue;if(norm(title)==='sanya'&&!authorMatches(label+' '+desc,author)&&!titleMatches(label,title))continue;add(out,`Wikidata: ${label||title}`,`${desc}${x.aliases?.length?' Aliases: '+x.aliases.join(', '):''}`,`https://www.wikidata.org/wiki/${x.id}`)}
  if(i===5)for(const x of d.response?.docs||[]){const returnedTitle=clean(x.title||'');const creator=Array.isArray(x.creator)?x.creator.join(' '):clean(x.creator||'');if(!titleMatches(returnedTitle,title)|| (creator&&!authorMatches(creator,author)))continue;add(out,`Internet Archive: ${x.title||title}`,`${creator} ${x.description||''}`,`https://archive.org/details/${x.identifier||''}`)}
 }
 if(trusted[key])for(const u of trusted[key]){try{const d:any=await json(u,7000);if(d.items)for(const x of d.items){const v=x.volumeInfo||{};if(titleMatches(v.title||title,title)&&(!v.authors||authorMatches(v.authors.join(' '),author)))add(out,'Trusted catalogue record',`${v.title||title}. Author: ${(v.authors||[author]).join(', ')}. ${v.description||''}`,x.selfLink)}else if(d.docs)for(const x of d.docs){if(titleMatches(x.title||'',title)&&(!x.author_name||authorMatches(x.author_name.join(' '),author)))add(out,'Trusted catalogue record',`${x.title||title}. Author: ${(x.author_name||[author]).join(', ')}. ${(x.first_sentence||[]).join(' ')}`)}else if(d.title||d.extract)add(out,'Trusted book page',`${d.title||title}. ${d.extract||''}`,u);else if(typeof d==='string')add(out,'Trusted book page',d,u);}catch{}}
 return out;
}
function parse(text:string):Question[]{const c=text.replace(/^```(?:json)?/i,'').replace(/```$/,'').trim(),a=c.indexOf('['),b=c.lastIndexOf(']');if(a<0||b<=a)throw Error('Invalid AI JSON');const p=JSON.parse(c.slice(a,b+1));return(Array.isArray(p)?p:[]).map((q:any)=>({question:clean(q.question),options:Array.isArray(q.options)?q.options.slice(0,4).map(clean):[],answer:Number(q.answer),explanation:clean(q.explanation)})).filter((q:Question)=>q.question&&q.options.length===4&&Number.isInteger(q.answer)&&q.answer>=0&&q.answer<4)}
async function ai(prompt:string){const ctl=new AbortController(),t=setTimeout(()=>ctl.abort(),12000);try{const r=await fetch('https://text.pollinations.ai/',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({messages:[{role:'user',content:prompt}],model:'openai',temperature:0.45}),signal:ctl.signal});if(!r.ok)throw Error(String(r.status));return parse(await r.text())}finally{clearTimeout(t)}}
const isMeta=(q:Question)=>{const s=norm(q.question);return /\b(author|written by|writer|title|published|publisher|publication date|year was.*published)\b/.test(s)};
function fallback(books:Book[],sources:Source[],count:number,seen:Set<string>){const out:Question[]=[];const contentSources=sources.filter(s=>s.title.toLowerCase().includes('content')||s.title.toLowerCase().includes('trusted book page')||s.text.length>220);for(const s of contentSources){const text=clean(s.text);if(text.length<120)continue;const b=books.find(x=>titleMatches(s.title,x.title)||text.includes(x.title));if(!b)continue;const q={question:`According to the verified book material, which statement best describes ${b.title}?`,options:[text.slice(0,180),`The book is unrelated to the subject described.`,`The source says the events did not occur.`,`The book contains no discussion of the subject.`],answer:0,explanation:'This fallback uses verified book-specific material rather than inventing a scene or fact.'};if(!seen.has(fp(q.question))&&!out.some(x=>similar(x.question,q.question)))out.push(q);if(out.length>=count)break}return out.slice(0,count)}
export async function POST(req:NextRequest){try{const body=await req.json(),books:Array<Book>=Array.isArray(body.books)?body.books.filter((b:any)=>clean(b?.title)&&clean(b?.author)).slice(0,5):[],requested=Math.min(100,Math.max(1,Number(body.questions)||10)),difficulty=clean(body.difficulty)||'Mixed',instructions=clean(body.instructions).slice(0,500),previous:Array<string>=Array.isArray(body.previousQuestions)?body.previousQuestions.map(String).slice(-80):[];if(!books.length)return NextResponse.json({error:'At least one book is required.'},{status:400});const sources=(await Promise.all(books.map(researchBook))).flat();const context=sources.map(s=>`SOURCE: ${s.title}\nURL: ${s.url||''}\n${s.text}`).join('\n\n').slice(0,140000);const seen=new Set(previous.map(fp).filter(Boolean)),accepted:Question[]=[];let metaCount=0;const metaLimit=Math.max(1,Math.floor(requested*0.2));const batchCount=requested<=12?3:requested<=40?4:5;const per=Math.ceil(requested/batchCount)+6;const prompts=Array.from({length:batchCount},(_,i)=>`You are EDUWILLS Book Intelligence AI, generation batch ${i+1}. BOOK IDENTITY LOCK: create questions ONLY for these exact title-author pairs: ${books.map(b=>`[TITLE=${b.title}] [AUTHOR=${b.author}]`).join('; ')}. Never interpret a title as a place, person, bay, city, company, website, song or another work with a similar name. If a title is ambiguous, the author is mandatory.\n\nCONTENT-FIRST REQUIREMENT: At least 80% of the questions MUST test the CONTENT of the selected book itself. Ask about specific plot events, characters, relationships, motivations, conflicts, setting, chronology, themes, symbols, important details, chapter events and supported literary inference. Questions such as 'Who wrote the book?', 'What is the title?', 'When was it published?' or similar author/title metadata do NOT count as content questions. At most 20% of the total may be author/title/publication/context questions. If you cannot support a content question from the evidence, DO NOT invent it.\n\nSOURCE PRIORITY: 1) saved book content/excerpts/summary/notes supplied with the request; 2) verified book-specific catalogue and trusted book pages; 3) other exact-title/exact-author research. Never use generic web knowledge.\n\nThe user's short test instruction is a HARD requirement and must influence every applicable question: ${instructions||'No extra instruction was supplied.'}\nDifficulty: ${difficulty}. Deliberately vary chapters, characters, themes and events across batches. Do not repeatedly ask the same fact in different wording. Do not invent quotations, chapters, scenes, characters or events. Exactly four plausible options and one correct answer. Include a concise explanation identifying the evidence basis. Return ONLY a JSON array of at most ${per} questions. RECENT QUESTIONS: ${previous.join(' | ')}\nBOOK CONTENT/RESEARCH EVIDENCE:\n${context}`);const batches=await Promise.allSettled(prompts.map(p=>ai(p)));for(const b of batches){if(b.status!=='fulfilled')continue;for(const q of b.value){const n=fp(q.question);if(!n||seen.has(n)||accepted.some(x=>similar(x.question,q.question)))continue;if(isMeta(q)&&metaCount>=metaLimit)continue;seen.add(n);accepted.push(q);if(isMeta(q))metaCount++;if(accepted.length>=requested)break}if(accepted.length>=requested)break}if(accepted.length<requested){const fb=fallback(books,sources,requested-accepted.length,seen);for(const q of fb){if(accepted.length>=requested)break;accepted.push(q)}}const contentCount=accepted.filter(q=>!isMeta(q)).length;const contentRatio=accepted.length?contentCount/accepted.length:0;return NextResponse.json({questions:accepted.slice(0,requested),sourcesUsed:sources.length,complete:accepted.length>=requested&&contentRatio>=0.8,generationMode:accepted.length>=requested&&contentRatio>=0.8?'ai-research':'research-fallback',contentRatio,notice:accepted.length<requested||contentRatio<0.8?'Not enough verified book-content evidence was available to safely create the requested content-focused quiz. EDUWILLS did not invent unsupported questions.':''});}catch(e:any){return NextResponse.json({error:e?.message||'Quiz AI unavailable.'},{status:500})}}
