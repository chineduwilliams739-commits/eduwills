'use client';

import { useEffect } from 'react';
import { ArrowLeft, Clock3, KeyRound, LayoutDashboard, Plus, ShieldCheck, Users, Settings } from 'lucide-react';

function makeToken() { const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789'; return Array.from({ length: 10 }, () => chars[Math.floor(Math.random() * chars.length)]).join(''); }

export default function AdminPage() {
  useEffect(() => { if (sessionStorage.getItem('eduwills_admin_auth') !== 'true') window.location.replace('/admin/login'); }, []);
  const generate = () => { const duration = (document.getElementById('duration') as HTMLSelectElement)?.value || '30 days'; const next = makeToken(); localStorage.setItem('eduwills_last_token', JSON.stringify({ token: next, duration, created: new Date().toISOString() })); alert(`WilliToken generated: ${next}\nDuration: ${duration}`); };
  return <main className="min-h-screen bg-slate-950 px-5 py-6 text-white sm:px-8"><div className="mx-auto max-w-6xl"><a href="/" className="inline-flex items-center gap-2 text-sm font-bold text-slate-300"><ArrowLeft size={17}/> EDUWILLS</a><div className="mt-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1.5 text-xs font-black uppercase tracking-wider text-cyan-200"><ShieldCheck size={14}/> Admin console</div><h1 className="mt-4 text-3xl font-black tracking-tight">EDUWILLS Administration</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-400">Generate WilliTokens, set activation duration and access administration tools.</p></div><a href="/admin/settings" className="inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm font-bold"><Settings size={17}/> Admin settings</a></div>
    <div className="mt-8 grid gap-5 lg:grid-cols-[1fr_1fr]"><section className="rounded-3xl border border-white/10 bg-white/5 p-6"><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-cyan-400/10 text-cyan-200"><KeyRound size={19}/></div><div><h2 className="font-black">Generate WilliToken</h2><p className="text-xs text-slate-400">Letters + numbers · admin controlled duration</p></div></div><label className="mt-6 block text-sm font-bold">Activation duration<select id="duration" defaultValue="30 days" className="mt-2 w-full rounded-xl border border-white/10 bg-slate-900 px-4 py-3 text-sm outline-none"><option>7 days</option><option>14 days</option><option>30 days</option><option>90 days</option><option>180 days</option><option>365 days</option></select></label><button onClick={generate} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-cyan-400 px-5 py-3.5 font-black text-slate-950"><Plus size={17}/> Generate WilliToken</button></section>
    <section className="rounded-3xl border border-white/10 bg-white/5 p-6"><h2 className="font-black">Admin tools</h2><div className="mt-5 grid gap-3 sm:grid-cols-2">{[[Users,'Users','View, activate, suspend and manage accounts'],[Clock3,'Activation history','See token creation and expiry history'],[LayoutDashboard,'Payment proofs','Review submitted receipts'],[ShieldCheck,'Admin settings','Change admin password']].map(([Icon,title,desc])=>{const I=Icon as typeof Users; return <a href={title === 'Admin settings' ? '/admin/settings' : '#'} key={title as string} className="rounded-2xl border border-white/10 bg-black/10 p-4 text-left hover:bg-white/5"><I size={18} className="text-cyan-200"/><div className="mt-4 text-sm font-black">{title as string}</div><div className="mt-1 text-xs leading-5 text-slate-400">{desc as string}</div></a>})}</div></section></div>
    <section className="mt-5 rounded-3xl border border-amber-500/20 bg-amber-500/5 p-6"><h2 className="font-black text-amber-200">Important for now</h2><p className="mt-2 text-sm leading-6 text-slate-400">This GitHub Pages version stores the admin session and generated token in the browser only. The temporary password is <strong className="text-white">admin</strong>. Before production, we should move authentication and token storage to a secure backend so users cannot inspect or alter them.</p></section></div></main>;
}
