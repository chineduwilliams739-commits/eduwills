import fs from 'node:fs';
const rulesPath='firestore.rules';let r=fs.readFileSync(rulesPath,'utf8');
r=r.replace("allow update: if isParticipant(chatId) && resource.data.senderId == request.auth.uid; allow delete: if isParticipant(chatId) && (resource.data.senderId == request.auth.uid || isAdmin());","allow update: if isParticipant(chatId) && ((resource.data.senderId == request.auth.uid && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['text','edited','editedAt','replyToId','replyToText','replyToName'])) || (isAdmin() && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['deleted','deletedAt','deletedBy','deletedByName','text','imageUrl']))); allow delete: if isParticipant(chatId) && (resource.data.senderId == request.auth.uid || isAdmin());");
r=r.replace("allow create, update, delete: if isParticipant(chatId) && request.resource.data.uid == request.auth.uid;","allow create, update: if isParticipant(chatId) && request.resource.data.uid == request.auth.uid; allow delete: if isParticipant(chatId) && resource.data.uid == request.auth.uid;");
r=r.replace("allow update: if signedIn() && resource.data.senderId == request.auth.uid; allow delete: if signedIn() && (resource.data.senderId == request.auth.uid || isAdmin());","allow update: if signedIn() && ((resource.data.senderId == request.auth.uid && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['text','edited','editedAt','replyToId','replyToText','replyToName'])) || (isAdmin() && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['deleted','deletedAt','deletedBy','deletedByName','text','imageUrl']))); allow delete: if signedIn() && (resource.data.senderId == request.auth.uid || isAdmin());");
r=r.replace("allow create, update, delete: if signedIn() && request.resource.data.uid == request.auth.uid;","allow create, update: if signedIn() && request.resource.data.uid == request.auth.uid; allow delete: if signedIn() && resource.data.uid == request.auth.uid;");
fs.writeFileSync(rulesPath,r);
const fn='functions/index.js';let f=fs.readFileSync(fn,'utf8');
if(!f.includes('exports.communityMessageUnread')){f="const { onDocumentCreated } = require('firebase-functions/v2/firestore');\n"+f;f+=`\n\nasync function incrementUnreadFor(collectionName, entityId, members, senderId) {\n  const db = getFirestore(); const ids=[...new Set((members||[]).filter(Boolean))].filter(uid=>uid!==senderId); if(!ids.length)return; const batch=db.batch();\n  for(const uid of ids){const ref=db.doc(\`${collectionName}/\${entityId}/readState/\${uid}\`);batch.set(ref,{unread:FieldValue.increment(1),updatedAt:FieldValue.serverTimestamp()},{merge:true});} await batch.commit();\n}\nexports.communityMessageUnread=onDocumentCreated({document:'communityGroups/{groupId}/messages/{messageId}',region:'us-central1'},async event=>{const snap=event.data;if(!snap)return;const d=snap.data()||{};const group=await getFirestore().doc(\`communityGroups/\${event.params.groupId}\`).get();if(!group.exists)return;await incrementUnreadFor('communityGroups',event.params.groupId,[...(group.data().memberIds||[]),...(group.data().adminIds||[])],d.senderId);});\nexports.chatMessageUnread=onDocumentCreated({document:'communityChats/{chatId}/messages/{messageId}',region:'us-central1'},async event=>{const snap=event.data;if(!snap)return;const d=snap.data()||{};const chat=await getFirestore().doc(\`communityChats/\${event.params.chatId}\`).get();if(!chat.exists)return;await incrementUnreadFor('communityChats',event.params.chatId,chat.data().participantIds||[],d.senderId);});\nexports.schoolMessageUnread=onDocumentCreated({document:'schoolCommunities/{schoolId}/messages/{messageId}',region:'us-central1'},async event=>{const snap=event.data;if(!snap)return;const d=snap.data()||{};const school=await getFirestore().doc(\`schoolCommunities/\${event.params.schoolId}\`).get();if(!school.exists)return;await incrementUnreadFor('schoolCommunities',event.params.schoolId,[...(school.data().memberIds||[]),...(school.data().adminIds||[])],d.senderId);});\n`;}
fs.writeFileSync(fn,f);console.log('Unread counters, reactions and secure sender/admin message permissions added.');
