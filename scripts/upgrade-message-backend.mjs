import fs from 'node:fs';

const rulesPath='firestore.rules';
let r=fs.readFileSync(rulesPath,'utf8');
r=r.replace("match /communityChats/{chatId}/messages/{messageId} { allow read: if isParticipant(chatId); allow create: if isActivated() && isParticipant(chatId) && request.resource.data.senderId == request.auth.uid; allow update, delete: if isParticipant(chatId) && resource.data.senderId == request.auth.uid; }","match /communityChats/{chatId}/messages/{messageId} { allow read: if isParticipant(chatId); allow create: if isActivated() && isParticipant(chatId) && request.resource.data.senderId == request.auth.uid; allow update: if isParticipant(chatId) && resource.data.senderId == request.auth.uid; allow delete: if isParticipant(chatId) && (resource.data.senderId == request.auth.uid || isAdmin()); }\n    match /communityChats/{chatId}/messages/{messageId}/reactions/{reactionId} { allow read: if isParticipant(chatId); allow create, update, delete: if isParticipant(chatId) && request.resource.data.uid == request.auth.uid; }\n    match /communityChats/{chatId}/readState/{uid} { allow read, create, update: if isParticipant(chatId) && request.auth.uid == uid; allow delete: if request.auth.uid == uid || isAdmin(); }");
r=r.replace("match /schoolCommunities/{schoolId} { allow read: if signedIn() && (isAdmin() || resource.data.status == 'active' && (request.auth.uid in resource.data.memberIds || request.auth.uid in resource.data.adminIds)); allow update: if isAdmin() || (signedIn() && request.auth.uid in resource.data.adminIds && request.resource.data.adminIds is list && request.resource.data.memberIds is list); allow delete: if isAdmin() || (signedIn() && request.auth.uid in resource.data.adminIds); }","match /schoolCommunities/{schoolId} { allow read: if signedIn() && (isAdmin() || resource.data.status == 'active' && (request.auth.uid in resource.data.memberIds || request.auth.uid in resource.data.adminIds)); allow update: if isAdmin() || (signedIn() && request.auth.uid in resource.data.adminIds && request.resource.data.adminIds is list && request.resource.data.memberIds is list); allow delete: if isAdmin() || (signedIn() && request.auth.uid in resource.data.adminIds); }\n    match /schoolCommunities/{schoolId}/messages/{messageId} { allow read: if signedIn() && (isAdmin() || request.auth.uid in get(/databases/$(database)/documents/schoolCommunities/$(schoolId)).data.memberIds || request.auth.uid in get(/databases/$(database)/documents/schoolCommunities/$(schoolId)).data.adminIds); allow create: if isActivated() && request.resource.data.senderId == request.auth.uid && (request.auth.uid in get(/databases/$(database)/documents/schoolCommunities/$(schoolId)).data.memberIds || request.auth.uid in get(/databases/$(database)/documents/schoolCommunities/$(schoolId)).data.adminIds); allow update: if signedIn() && resource.data.senderId == request.auth.uid; allow delete: if signedIn() && (resource.data.senderId == request.auth.uid || isAdmin()); }\n    match /schoolCommunities/{schoolId}/messages/{messageId}/reactions/{reactionId} { allow read: if signedIn(); allow create, update, delete: if signedIn() && request.resource.data.uid == request.auth.uid; }\n    match /schoolCommunities/{schoolId}/readState/{uid} { allow read, create, update: if signedIn() && request.auth.uid == uid; allow delete: if request.auth.uid == uid || isAdmin(); }");
fs.writeFileSync(rulesPath,r);

const fn='functions/index.js';
let f=fs.readFileSync(fn,'utf8');
if(!f.includes('exports.communityMessageUnread')){
 f="const { onDocumentCreated } = require('firebase-functions/v2/firestore');\n"+f;
 f += `\n\nasync function incrementUnreadFor(collectionName, entityId, members, senderId) {\n  const db = getFirestore();\n  const ids = [...new Set((members || []).filter(Boolean))].filter(uid => uid !== senderId);\n  if (!ids.length) return;\n  const batch = db.batch();\n  for (const uid of ids) {\n    const ref = db.doc(\`${collectionName}/\${entityId}/readState/\${uid}\`);\n    batch.set(ref, { unread: FieldValue.increment(1), updatedAt: FieldValue.serverTimestamp() }, { merge: true });\n  }\n  await batch.commit();\n}\n\nexports.communityMessageUnread = onDocumentCreated({ document: 'communityGroups/{groupId}/messages/{messageId}', region: 'us-central1' }, async event => {\n  const snap = event.data; if (!snap) return; const d = snap.data() || {};\n  const group = await getFirestore().doc(\`communityGroups/\${event.params.groupId}\`).get();\n  if (!group.exists) return;\n  await incrementUnreadFor('communityGroups', event.params.groupId, [...(group.data().memberIds || []), ...(group.data().adminIds || [])], d.senderId);\n});\n\nexports.chatMessageUnread = onDocumentCreated({ document: 'communityChats/{chatId}/messages/{messageId}', region: 'us-central1' }, async event => {\n  const snap = event.data; if (!snap) return; const d = snap.data() || {};\n  const chat = await getFirestore().doc(\`communityChats/\${event.params.chatId}\`).get();\n  if (!chat.exists) return;\n  await incrementUnreadFor('communityChats', event.params.chatId, chat.data().participantIds || [], d.senderId);\n});\n\nexports.schoolMessageUnread = onDocumentCreated({ document: 'schoolCommunities/{schoolId}/messages/{messageId}', region: 'us-central1' }, async event => {\n  const snap = event.data; if (!snap) return; const d = snap.data() || {};\n  const school = await getFirestore().doc(\`schoolCommunities/\${event.params.schoolId}\`).get();\n  if (!school.exists) return;\n  await incrementUnreadFor('schoolCommunities', event.params.schoolId, [...(school.data().memberIds || []), ...(school.data().adminIds || [])], d.senderId);\n});\n`;
}
fs.writeFileSync(fn,f);
console.log('Unread counters, reactions and sender/admin message permissions added.');
