import fs from 'node:fs';
const path='app/dashboard/quiz/page.tsx';
let s=fs.readFileSync(path,'utf8');
if(!s.includes('if (setup && quizLoading && !qs.length)')){
 const marker='  if (setup && qs.length) {';
 const block=`  if (setup && quizLoading && !qs.length) {\n    return <main className="min-h-screen bg-gradient-to-b from-slate-50 via-white to-cyan-50 text-ink"><div className="mx-auto flex min-h-screen max-w-2xl items-center justify-center px-5 py-10"><section className="w-full rounded-[2rem] border border-slate-200 bg-white p-7 text-center shadow-xl sm:p-10"><div className="mx-auto grid h-16 w-16 place-items-center rounded-2xl bg-indigo-50 text-indigo-600"><Loader2 className="animate-spin" size={30}/></div><p className="mt-5 text-xs font-black uppercase tracking-[.2em] text-eduBlue">EDUWILLS • QUIZ STUDIO</p><h1 className="mt-2 text-2xl font-black">Building your quiz…</h1><p className="mt-3 text-sm leading-6 text-slate-500">Checking cached questions first and generating only what is missing.</p><div className="mx-auto mt-6 h-2 max-w-sm overflow-hidden rounded-full bg-slate-100"><div className="h-full w-1/2 animate-pulse rounded-full bg-gradient-to-r from-indigo-600 to-cyan-500"/></div></section></div></main>;\n  }\n\n  if (setup && !qs.length && quizError) {\n    return <main className="min-h-screen bg-gradient-to-b from-slate-50 via-white to-cyan-50 text-ink"><div className="mx-auto flex min-h-screen max-w-2xl items-center justify-center px-5 py-10"><section className="w-full rounded-[2rem] border border-amber-200 bg-white p-7 shadow-xl sm:p-10"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-amber-50 text-amber-600"><XCircle size={28}/></div><p className="mt-5 text-xs font-black uppercase tracking-[.2em] text-eduBlue">EDUWILLS • QUIZ STUDIO</p><h1 className="mt-2 text-2xl font-black">Quiz generation could not finish</h1><p className="mt-3 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm font-bold leading-6 text-amber-900">{quizError}</p><div className="mt-6 flex flex-wrap gap-3"><button onClick={() => generate(setup)} className="inline-flex flex-1 items-center justify-center gap-2 rounded-xl bg-gradient-to-r from-indigo-600 to-cyan-500 px-5 py-3 font-black text-white"><Sparkles size={17}/> Retry generation</button><button onClick={resetQuiz} className="flex-1 rounded-xl border border-slate-200 bg-white px-5 py-3 font-black">Back to books</button></div></section></div></main>;\n  }\n\n`;
 if(!s.includes(marker)) throw new Error('Quiz Studio render insertion point not found');
 s=s.replace(marker,block+marker);
 fs.writeFileSync(path,s);
}
console.log('Book Learner generation now remains visible while loading and after errors.');
