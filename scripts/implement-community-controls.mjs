import fs from 'node:fs';

// Deterministic source repair. Safe to run repeatedly.

const group='app/dashboard/community/group/page.tsx';
let s=fs.readFileSync(group,'utf8');
s=s.replace("[memberSearch,setMemberSearch]=useState(''),[reply,setReply]=useState<any>(null),[unread,setUnread]=useState(0);", "[memberSearch,setMemberSearch]=useState(''),[reply,setReply]=useState<any>(null),[unread,setUnread]=useState(0),[isMember,setIsMember]=useState(false),[joining,setJoining]=useState(false);");
s=s.replace("setG(d);setName(d.name||'');setDesc(d.description||'');setAvatar(d.avatarUrl||'');setCover(d.coverImageUrl||'');setUnderstood(d.creatorRulesAccepted===true||d.ownerId!==user.uid)", "setG(d);setIsMember(d.ownerId===user.uid||d.adminIds?.includes(user.uid)||d.memberIds?.includes(user.uid));setName(d.name||'');setDesc(d.description||'');setAvatar(d.avatarUrl||'');setCover(d.coverImageUrl||'');setUnderstood(d.creatorRulesAccepted===true||d.ownerId!==user.uid)");
s=s.replace("useEffect(()=>{if(!g||!user||!understood)return;return onSnapshot(query(collection(db,'communityGroups',id,'messages'),orderBy('createdAt','asc'),limit(200)),s=>setMessages(s.docs.map(x=>({id:x.id,...x.data()}))))},[g?.id,understood]);", "useEffect(()=>{if(!g||!user||!understood||!isMember)return;return onSnapshot(query(collection(db,'communityGroups',id,'messages'),orderBy('createdAt','asc'),limit(200)),s=>setMessages(s.docs.map(x=>({id:x.id,...x.data()}))))},[g?.id,understood,isMember]);");
s=s.replace("const isAdmin=!!g?.adminIds?.includes(user?.uid),isOwner=g?.ownerId===user?.uid;", "const isAdmin=!!g?.adminIds?.includes(user?.uid),isOwner=g?.ownerId===user?.uid,isLocked=g?.messagingLocked===true;");
s=s.replace("async function acknowledge(){", "async function joinGroup(){if(!user||!g||joining)return;setJoining(true);try{await updateDoc(doc(db,'communityGroups',id),{memberIds:arrayUnion(user.uid),updatedAt:serverTimestamp()});setIsMember(true);setNotice('You joined the group.');}catch(e:any){setNotice(e?.message||'Could not join this group.')}finally{setJoining(false)}}\n async function toggleLock(){if(!isAdmin)return;try{await updateDoc(doc(db,'communityGroups',id),{messagingLocked:!isLocked,updatedAt:serverTimestamp()});setNotice(!isLocked?'Sending messages is now locked for members.':'Sending messages is now unlocked.')}catch(e:any){setNotice(e?.message||'Could not change message lock.')}}\n async function acknowledge(){");
s=s.replace("async function send(){if((!draft.trim()&&!image)||!user)return;", "async function send(){if((!draft.trim()&&!image)||!user||!isMember||isLocked)return;");
const gate="\n if(!isMember)return <main className=\"min-h-screen bg-paper p-6\"><a href={BASE+'/dashboard/community/'} className=\"inline-flex items-center gap-2 rounded-xl border bg-white px-4 py-3 text-sm font-black\"><ArrowLeft size={17}/> Community</a><div className=\"mx-auto mt-12 max-w-xl rounded-3xl bg-white p-8 text-center shadow-sm\"><Users className=\"mx-auto text-cyan-600\" size={44}/><h1 className=\"mt-4 text-2xl font-black\">Join {g.name}</h1><p className=\"mt-2 text-sm leading-6 text-slate-500\">You can see the group details, but messages and sending are available only after you join.</p><button onClick={joinGroup} disabled={joining} className=\"mt-6 w-full rounded-xl bg-ink px-5 py-4 text-sm font-black text-white disabled:opacity-50\">{joining?'Joining…':'JOIN GROUP'}</button></div></main>;\n";
s=s.replace("if(!understood)return <main", gate+"\n if(!understood)return <main");
s=s.replace("<section className=\"mx-3 my-3 rounded-2xl border border-slate-200 bg-white p-3 shadow-sm sm:mx-0\"><div", "{isAdmin&&<section className=\"mx-3 mt-3 rounded-2xl border border-cyan-200 bg-white p-4 shadow-sm sm:mx-0\"><div className=\"flex items-center justify-between gap-3\"><div><p className=\"text-[10px] font-black uppercase tracking-wider text-cyan-700\">MESSAGE CONTROL</p><p className=\"mt-1 text-xs font-bold text-slate-500\">{isLocked?'Members cannot send messages.':'Members can send messages.'}</p></div><button onClick={toggleLock} className=\"rounded-xl bg-ink px-4 py-2.5 text-xs font-black text-white\">{isLocked?'UNLOCK SENDING':'LOCK SENDING'}</button></div></section>}\n <section className=\"mx-3 my-3 rounded-2xl border border-slate-200 bg-white p-3 shadow-sm sm:mx-0\"><div");
s=s.replace("<section className=\"mx-3 mb-3 rounded-2xl", "{isLocked&&<div className=\"mx-3 mb-3 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-xs font-black text-amber-900 sm:mx-0\">Sending is locked by a group admin. You can still read the conversation.</div>}\n <section className=\"mx-3 mb-3 rounded-2xl");
s=s.replace("<button onClick={send} disabled={!draft.trim()&&!image}", "<button onClick={send} disabled={isLocked||(!draft.trim()&&!image)}");
fs.writeFileSync(group,s);

const chat='app/dashboard/community/chat/page.tsx';
let c=fs.readFileSync(chat,'utf8');
c=c.replace("[busy,setBusy]=useState(false),[reply,setReply]=useState<any>(null);", "[busy,setBusy]=useState(false),[reply,setReply]=useState<any>(null),[searching,setSearching]=useState(false),[searched,setSearched]=useState(false);");
c=c.replace("<input value={search} onChange={e=>setSearch(e.target.value)} placeholder=\"Search by name or username…\" className=\"min-w-0 flex-1 bg-transparent text-sm font-bold outline-none\"/>", "<input value={search} onChange={e=>setSearch(e.target.value)} placeholder=\"Search by name or username…\" className=\"min-w-0 flex-1 bg-transparent text-sm font-bold outline-none\"/><button onClick={()=>setSearched(true)} className=\"rounded-lg bg-ink px-3 py-2 text-[10px] font-black text-white\">Search</button>");
c=c.replace("{results.length>0&&<div className=\"border-b p-2\">", "{searched&&!results.length&&<div className=\"px-4 pb-3 text-xs font-bold text-slate-400\">Nothing found for “{search}”.</div>}{results.length>0&&<div className=\"border-b p-2\">");
fs.writeFileSync(chat,c);

const personal='app/dashboard/personal/page.tsx';
let p=fs.readFileSync(personal,'utf8');
p=p.replace("const normalize=(v:any)=>{const x=String(v||'').toLowerCase().trim();if(x==='primary'||x==='pupil'||x==='pupils')return'primary';if(x==='junior'||x==='jss'||x==='junior-secondary'||x==='junior secondary')return'junior';if(x==='senior'||x==='sss'||x==='senior-secondary'||x==='senior secondary')return'senior';return'book'};", "const normalize=(v:any)=>{const x=String(v||'').toLowerCase().trim();if(x==='primary'||x==='pupil'||x==='pupils')return'primary';if(x==='junior'||x==='jss'||x==='junior-secondary'||x==='junior secondary')return'junior';if(x==='senior'||x==='sss'||x==='senior-secondary'||x==='senior secondary')return'senior';if(x==='book'||x==='books'||x==='book-learner'||x==='book learner')return'book';return''}; const valuesOf=(v:any)=>{if(Array.isArray(v))return v;if(typeof v==='string'){try{const a=JSON.parse(v);if(Array.isArray(a))return a}catch{}return v.split(',').map(x=>x.trim()).filter(Boolean)}return v?[v]:[]};");
p=p.replace("const values=[...(Array.isArray(profile.categories)?profile.categories:[]),...(Array.isArray(profile.educationLevels)?profile.educationLevels:[]),...(Array.isArray(profile.schoolLevels)?profile.schoolLevels:[]),profile.category,profile.educationLevel,profile.schoolLevel].map(normalize);", "const raw=[...valuesOf(profile.categories),...valuesOf(profile.activeCategories),...valuesOf(profile.pendingActivationCategories),...valuesOf(profile.educationLevels),...valuesOf(profile.schoolLevels),...valuesOf(profile.paidCategories),profile.category,profile.educationLevel,profile.schoolLevel,profile.activeCategory,profile.activeCategoryId];const values=raw.map(normalize).filter(Boolean);");
fs.writeFileSync(personal,p);

const upload='components/DeviceImageUpload.tsx';
let u=fs.readFileSync(upload,'utf8');
u=u.replace("import {getDownloadURL,ref,uploadBytes} from 'firebase/storage';", "import {getDownloadURL,ref,uploadBytesResumable} from 'firebase/storage';");
u=u.replace("if(file.size<=512*1024)return file;", "if(file.size<=256*1024)return file;");
u=u.replace("const bitmap=await createImageBitmap(file);const max=1280;", "const bitmap=await createImageBitmap(file);const max=900;");
u=u.replace("canvas.toBlob(resolve,'image/webp',.7)", "canvas.toBlob(resolve,'image/webp',.68)");
u=u.replace("const timer=setTimeout(()=>{if(!finished)reject(Object.assign(new Error('IMAGE_UPLOAD_TIMEOUT'),{code:'storage/retry-limit-exceeded'}))},45000);", "const timer=setTimeout(()=>{if(!finished)reject(Object.assign(new Error('IMAGE_UPLOAD_TIMEOUT'),{code:'storage/retry-limit-exceeded'}))},30000);");
u=u.replace("onProgress(12);\n  uploadBytes(storageRef,file,{contentType:file.type,customMetadata:{source:'device'}}).then(()=>{finished=true;clearTimeout(timer);onProgress(96);resolve()}).catch(error=>{finished=true;clearTimeout(timer);reject(error)});", "onProgress(2); const task=uploadBytesResumable(storageRef,file,{contentType:file.type,customMetadata:{source:'device'}}); task.on('state_changed',snap=>{onProgress(Math.min(95,Math.round((snap.bytesTransferred/snap.totalBytes)*95)));},error=>{finished=true;clearTimeout(timer);reject(error);},()=>{finished=true;clearTimeout(timer);onProgress(96);resolve()});");
u=u.replace("try{await current.reload()}catch{}\n  if(!auth.currentUser)", "if(!auth.currentUser)");
while(u.includes("setMessage('Preparing secure upload…');setMessage('Preparing secure upload…');"))u=u.replace("setMessage('Preparing secure upload…');setMessage('Preparing secure upload…');","setMessage('Preparing secure upload…');");
fs.writeFileSync(upload,u);

const rules='firestore.rules';
let r=fs.readFileSync(rules,'utf8');
r=r.replace("allow create: if isActivated() && isGroupMember(groupId) && request.resource.data.senderId == request.auth.uid;", "allow create: if isActivated() && isGroupMember(groupId) && get(/databases/$(database)/documents/communityGroups/$(groupId)).data.messagingLocked != true && request.resource.data.senderId == request.auth.uid;");
r=r.replace("allow create: if isActivated() && isSchoolMember(schoolId) && request.resource.data.senderId == request.auth.uid;", "allow create: if isActivated() && isSchoolMember(schoolId) && get(/databases/$(database)/documents/schoolCommunities/$(schoolId)).data.messagingLocked != true && request.resource.data.senderId == request.auth.uid;");
if(!r.includes("request.resource.data.memberIds.size() == resource.data.memberIds.size() + 1")) r=r.replace("allow update: if isGroupAdmin(groupId) && request.resource.data.adminIds is list", "allow update: if signedIn() && !isGroupMember(groupId) && request.resource.data.memberIds is list && request.resource.data.memberIds.size() == resource.data.memberIds.size() + 1 && request.auth.uid in request.resource.data.memberIds && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['memberIds','updatedAt']); allow update: if isGroupAdmin(groupId) && request.resource.data.adminIds is list");
fs.writeFileSync(rules,r);
